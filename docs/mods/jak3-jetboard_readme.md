# Mod Readme — Jak 3 Jetboard Mechanics Port to Jak 2

> **Game / Jeu :** Jak 2  
> **Branch / Branche :** `jak2/features/jak3-jetBoard`  
> **Target Subsystem / Sous-système :** Target Jetboard (`goal_src/jak2/engine/target/board/`) & Sound Bank / Art Group Pipelines

---

## Language Selection / Sélection de la langue
- [English Version](#-english-version)
- [Version Française](#-version-française)

---

# 🇬🇧 English Version

## 1. Description & Features

This mod ports three core Jetboard mechanics introduced in Jak 3 into Jak 2's native jetboard system:

1. **Chargeable High Jump:**
   - **Controls:** Hold `L1` (crouch on board) then release `X` / Jump.
   - **Behavior:** Charges dynamic upward kinetic energy and launches Jak much higher than standard board jumps (`jakb-board-jump-high-ja`). Plays dedicated charge & launch audio.
2. **Board Zap Attack (Area-of-Effect & I-Frames):**
   - **Controls:** Press `Circle` while riding the board.
   - **Behavior:** Unleashes a radial electric shockwave dealing damage to nearby enemies with invincibility frames during the discharge. Plays `BOARD_ZAP` and `BOARD_ZAP_HIT`.
3. **180-Degree Quick Turn-Around:**
   - **Controls:** Press `Triangle` while riding the board.
   - **Behavior:** Executes a rapid 180° snap rotation (`jakb-board-turn-around-ja`). Upon completing the rotation, triggers a speed boost forward with vibration, landing dust particles, and the native `"board-boost"` sound effect (matching the spin-trick boost reward).

---

## 2. Technical Architecture & Tooling

### A. Animation Retargeting Pipeline (`retarget_anim` & `CMakeLists.txt`)
To cleanly import Jak 3 animations into Jak 2 without manual Blender edits or hex manipulation, a dedicated standalone C++ tool was integrated into the OpenGOAL toolchain:

- **`goalc/CMakeLists.txt` :**
  - Declares executable target `retarget_anim` (`add_executable(retarget_anim retarget_anim/main.cpp retarget_anim/retarget_anim.cpp)`).
  - Links `common`, `fmt`, and `tiny_gltf` to directly read, process, and write binary glTF/GLB containers.
- **`goalc/retarget_anim/main.cpp` :**
  - Provides a CLI interface (via `CLI11`) to configure retargeting:
    - `--base` (`-b`): Base reference GLB carrying Jak 2's native mesh and skeleton (`jakb-lod0.glb` / `daxter-lod0.glb`).
    - `--source` (`-s`): Source GLB containing raw extracted animations from Jak 3.
    - `--anim` (`-a`): Exact animation names to retarget (`board-jump-high`, `board-turn-around`).
    - `--root-joints`: Joints receiving true root motion translation and rotation (`align`, `main`).
    - `--neutral-scale-joints`: Sockets protected from scale drift (`board`).
- **`goalc/retarget_anim/retarget_anim.cpp` :**
  - **Bone-by-name matching:** Maps animation tracks to Jak 2 skeleton joints by semantic name, bypassing joint index discrepancies.
  - **Stretching elimination:** Retains translation only on root bones (`align`/`main`) and forces pure rotation (quaternions) on all child bones to preserve natural limb lengths.
  - **Attachment scale preservation:** Keeps native bind-pose scale on joint 25 `board` (`(0.7143, 0.7143, 0.7143)` canceling `main`'s `1.4` scale).
  - **GLTF spec compliance:** Automatically converts `JOINTS_0` accessor from `UNSIGNED_INT` to `UNSIGNED_BYTE` for `gltf_util` compatibility and purges orphaned buffer data.

### B. Sound Bank Injection (`SBK` Tooling)
- **`decompiler/data/extract_sbk.cpp` & `goalc/build_sbk/` :** C++ tools to extract ADPCM audio from `.SBK` banks and inject new samples via `append-sbk` macro in `project-lib.gp`.
- **`custom_assets/jak2/sounds/sfx/MODEBORD/` :** Provides the 4 Jak 3 audio files (`BOARD_CHARGE`, `BOARD_LAUNCH`, `BOARD_ZAP`, `BOARD_ZAP_HIT`).

### C. Dynamic Art Linking & GOAL Gameplay Logic
- **`goal_src/jak2/engine/anim/joint.gc` :** Hook inside `art-group::relocate` to dynamically link custom art groups at file load time (`link-art!`).
- **`goal_src/jak2/engine/target/board/board-h.gc` :** Extended `board-info` and `target-board-bank` to hold charge timers, zap duration, and sound IDs.
- **`goal_src/jak2/engine/target/board/board-states.gc` & `target-board.gc` :** Implemented high jump, quick turnaround, and zap attack state machines with radial damage collision and i-frames.

---

## 3. How to Test & Play

1. Set the active game to Jak 2:
   ```bash
   task set-game-jak2
   ```
2. Build custom sound assets and recompile:
   ```bash
   task extract
   ```
3. In REPL (`task repl`), hot-recompile code:
   ```lisp
   (mi)
   ```
4. Boot the game:
   ```bash
   task boot-game
   ```
5. Equip Jetboard (`R2`) and test:
   - **Charge Jump:** Hold `L1`, release `X`.
   - **Zap Attack:** Press `Circle`.
   - **Quick Turn:** Press `Triangle`.

---

## 4. Current Status & Investigations

- **Compile & Boot Status:** Full compilation and boot verified on Jak 2 with all 3 mechanics operational.
- **Visual Glitches Fixed at Source (1st playtest round):**
  - *Board scale fix:* The generator previously hardcoded `board` scale to `(1,1,1)`. In Jak 2's native bind-pose, `board` has scale `(0.7143,0.7143,0.7143)` as a child of `main` (scale `1.4`), canceling out the parent scaling. Fixed by retaining native bind-pose scale.
  - *Turn-around rotation fix:* Removed forced 180° yaw on `align` in the GLB, as `target-board-turn-around` (`board-states.gc`) already drives the 180° rotation entirely through gameplay code (`quaternion-rotate-y!` and `align!`).
- **Particle Effects Fixed (2nd playtest round):**
  - *Missing charge-up glow:* The `charge-part` struct field existed but no `create-launch-control` or per-frame `spawn` call had ever been wired up. Added `group-target-board-duck-charge` (id 1295) to `board-part.gc`, created the launch control in `target-board-init`, and added the per-frame `spawn` call in `target-board-real-post` (gated on `charge-progress != 0`) — mirrors Jak 3's own logic exactly (`target-board.gc:2215-2224`).
  - *Wrong zap effect:* The zap attack was mistakenly reusing `group-board-spin-attack`, the native spin-trick effect, unrelated to the zap. Added a dedicated `group-board-zap-attack` (id 1296) and moved the attach point from a hand-computed offset to the `hips` joint (index 25 in Jak 2, 37 in Jak 3 — same joint name, different skeleton index, confirmed by direct inspection of both skeletons), matching Jak 3's real implementation.
  - *Missing textures:* Jak 3's original effects reference textures (`laser-hit2`, `laser-hit-rim`) absent from Jak 2's asset set (confirmed via `decompiler/config/jak2/ntsc_v1/tex-info.min.json`). Substituted with `starflash` and `water-ring`, both already native to Jak 2 (`water-ring` is already used by the native `group-board-spin-attack`) — this is the same substitution strategy the `og-j1-board` mod used for the identical problem, not an improvised choice.
- **Cosmetic Note:** Console output `-debug` prints `no eye anim data for target/sidekick` on new animations because there are no eye blink table entries for custom animation clips (neutral eye pose fallback, harmless).
- **Crash + Joint Fixes (3rd playtest round):**
  - *Zap attack crash (game-breaking):* `part-tracker-spawn`'s `:target` argument is broken in Jak 2's macro — `part-tracker-init` (`generic-obs.gc`) casts the macro's constant `*launch-matrix*` pointer straight to an `int` for `target-joint` whenever `:target` is set, regardless of what `:mat-joint` was, producing a garbage joint index and an out-of-bounds `node-list` read on every zap. On top of that, the custom `board-zap-track` particle callback read `node-list` off `key proc`, which for a `part-tracker`-spawned effect is the ephemeral tracker process itself (no skeleton, no `node-list`) — a second, independent null dereference. Jak 3's own call (`:mat-joint 37` as a raw int, with an explicit tracker-type argument) relies on a different, incompatible macro signature, so it wasn't portable as-is. Fixed by dropping `:target` and the custom callback entirely, following Jak 2's own working native precedent (`group-board-spin-attack`, spawned the same way with a plain snapshotted `:mat-joint` vector and `sparticle-track-root` instead of a custom joint lookup).
  - *Charge glow anchored to the wrong joint:* `board-charge-track` used joint index 25 for "board", but direct inspection of `jakb-lod0.glb`'s skin joint order shows index 24 = `board`, 25 = `hips` — the glow was actually following the hips. Corrected to 24.
- **Positioning fixes (4th playtest round):** the crash fix above visibly worked (charge glow visible, zap effect matches Jak 3), but surfaced 2 more precise issues:
  - *Charge glow position still wrong even at index 24* (landed near the head, not the board) — proof that hand-counting a GLB's skin joint list doesn't reliably match this joint-group's real compiled index (got it wrong twice: 25, then 24). Replaced both `board-charge-track`'s hardcoded index and the zap trigger's with `(joint-node-index jakb-lod0-jg board)` / `(joint-node jakb-lod0-jg hips)` — a compile-time name-based lookup against the real `jakb-lod0-jg` joint-group data, the same mechanism every other native joint lookup in the game already relies on, instead of a manually-counted number.
  - *Turn-around: momentum doesn't reverse, character sinks into the ground, animation looks broken* — deep investigation, not yet resolved at the time. Compared `target-board-turn-around` line-by-line against Jak 3's native state: the 3 apparent differences (`compute-alignment!` vs `compute-delta-align!`, the `target-align-vel-z-adjust`-wrapped velocity formula, `dynam` vs `standard-dynamics`) all turned out to be correct, necessary Jak 2 API adaptations (`compute-delta-align!` doesn't exist in Jak 2 at all; the other two are Jak 2's own native idiom, used identically in `target.gc`/`target-darkjak.gc`/`target-indax.gc`). The velocity-reversal formula reads `(-> self align delta trans z)`, which `compute-alignment!` derives from joint index 1, believed at the time to be `prejoint`. Re-ran `retarget_anim` with `prejoint` added to `--root-joints` to test the obvious hypothesis - no change. **This joint identification turned out to be wrong — see the 6th round below.** Added temporary debug logging (`[turn-around-debug]` prefix) to get real runtime numbers on the next test instead of more static theorizing.
  - *Charged jump: character looks abnormally stretched throughout the whole animation* — not yet investigated to a conclusion. Confirmed `target-board-jump`'s high-jump branch does *not* use `compute-alignment!`/`align!` at all - height comes from physics (`transv`/gravity) with the animation's own playback speed scrubbed via `root-channel 0 param 0/1` to stay visually in sync with the physics arc (`time-to-apex`). That makes this a different category of problem from the turn-around issue above - most likely pose/rotation retargeting fidelity on individual limb joints not suiting Jak 2's proportions, rather than a root-motion/alignment bug. No fix attempted yet.
  - *Zap effect doesn't follow the board* — expected given the crash-fix design (a static one-time snapshot position, matching the native `group-board-spin-attack` pattern used for a `part-tracker-spawn`-owned effect). Attempted fix: switched to a dedicated `zap-part` launch control created directly on the target (mirroring `charge-part`) and spawned once per trigger, so `board-zap-track` could safely re-read the hips joint's live position every particle-update. **This broke the zap entirely (move and effect both stopped happening) and was reverted** - see the 5th playtest round below.
- **Regression (5th playtest round): the zap-part rework above broke the move completely.** Reverted `board-h.gc`/`board-part.gc`/`target-board.gc` back to the exact 4th-round (crash-fix) version: `part-tracker-spawn` with a plain snapshotted `:mat-joint` vector, `sparticle-track-root` as the particle `:func`, no `zap-part` field. Likely mechanism (not fully confirmed, but consistent with the symptom): `zap-part` was a *brand-new* struct field, unlike `charge-part` (added earlier, already proven stable across sessions) - the lazy-init guard `(if (zero? (-> self board zap-part)) (create-launch-control ...))` assumes that memory starts zeroed the first time, but a freshly-inserted field in a pooled process struct can start out holding leftover content from whatever previously occupied that memory. If the zero-check read that as non-zero, `create-launch-control` would never run, leaving `zap-part` pointing at garbage - spawning through it would silently do nothing rather than crash, which fits "disappeared" better than a crash would.
- **Sound logging cleanup (5th playtest round):** the blanket by-name lookup-failure logging added 2 rounds ago fired for every sound in the game, drowning the actually-relevant lines under constant, unrelated, pre-existing failures for ambient sounds (`PORT_AMB1`, `BARGE_ENGINE`, `BOW_WASH`, ...) that have nothing to do with this mod. Scoped `player.cpp`'s logging to fire only when the sound name contains "BOARD" (a plain substring check is enough - `sound_name` is already uppercased by `strcpy_toupper` before this RPC is issued). Also added a one-line, one-time-per-session GOAL log confirming the `sound-bank-load("board")` call from the previous fix actually executes, since the C++ side alone can't distinguish "board bank loads fine, individual sound names just don't match" from "the load call never even runs".
- **Turn-around — root cause found and fixed (6th playtest round).** The 4th round's `prejoint` identification for joint index 1 was wrong — it was reached by counting the GLB's raw joint array, the same mistake already made twice on the `board`/`hips` joint. Rebuilt the mapping from the actual GLTF parent/child hierarchy instead (not array position), using `build_actor.cpp`'s own indexing rule (game joint index = GLTF skin index + 1, `goalc/build_actor/common/build_actor.cpp`): index 1 is actually **`align`** (parentless and childless - genuinely isolated, as previously thought), not `prejoint`, which is index 2 (also parentless within the skin, but with `main` as its one child). The `[turn-around-debug]` log confirmed `align delta trans` is exactly `(0,0,0)` on every single frame of every turn attempted; a direct channel dump of the imported GLB then confirmed this is faithful to Jak 3's own source data — `align` carries zero translation keys in both imported clips, all real root motion is on `main` instead — so this was never a retargeting gap, on this port or the 4th round's. With the velocity formula's scaling factor always 0, it was zeroing the player's entire horizontal speed for the whole state instead of merely modulating it: the character free-fell straight down with no horizontal control for up to a full second on every attempt. The freeze reported after this round's test is the same bug compounding: the move visibly does nothing useful, so pressing Triangle again (a natural reaction) re-triggers it - the log shows at least 5 separate turn-around executions chained back to back with `pos-y` net-decreasing throughout, reaching roughly -10244 (raw units) by the time the log was captured, with brief `parking-spot::find-ground` ground contacts between attempts rather than a clean landing. Fixed the formula to match Jak 3's native math exactly (dropped the `target-align-vel-z-adjust` wrapper, which Jak 3 doesn't use here) and added a fallback: when the animation provides no root-motion signal, hold the entry speed instead of zeroing it, so the player keeps moving through the turn instead of stalling in place. This is a deliberate Jak-2-specific adaptation, not a literal port of Jak 3's formula, since the underlying data gap (no `align` motion) appears to be inherent to Jak 3's own source clip rather than something a retargeting fix could recover. Removed the now-explained `[turn-around-debug]` logging. **Not yet re-tested.**
- **Gun joint appears to drift away from the skeleton during both imported animations (6th round hypothesis, tested and disproven in the 7th).** The 6th round's theory - uncorrected rest/bind-pose mismatch between Jak 2 and Jak 3 compounding down `gun`'s parent chain (`gun → upper_body → waist_prog → main → prejoint`) - was implemented (rotation now retargeted as a delta from the source's own bind pose instead of a raw copy, `retarget_anim.cpp`, compiles clean) and regenerated into both GLBs, but a direct check of the actual bind-pose rotations for every joint in that chain, in both Jak 2's and Jak 3's native `jakb-lod0.glb`, found them **identical** `(0,0,0,1)` (pure identity) on both sides. With identical bind poses the delta computation is mathematically the same as a raw copy, which is exactly what was measured: a forward-kinematics comparison of `gun`'s world position relative to `main` across every frame, old retargeting vs new, came back byte-identical. **The rest-pose-mismatch hypothesis is wrong.** The fix is kept anyway (harmless, and strictly more correct for any future joint pair that does have mismatched bind poses), but it does not address this symptom. The FK check also didn't turn up anything obviously wrong in the data itself: `gun`'s distance from `main` traces a smooth, continuous, bounded arc (roughly 1.03-1.16m over the jump-high clip, no jumps or outliers) - which reads as ordinary arm-swing motion, not a bug. Since `gun` has no attached mesh to judge it against in Blender (confirmed - it's bone-only), it's possible this is genuine, intentional source motion that just looks like unexplained drift in isolation. **Need a screenshot or short clip of what this actually looks like to make further progress** - out of static-analysis leads on this one.
- **Audio — real root cause found and fixed (7th playtest round).** The new `[board-sound-debug]` logging pinpointed it immediately: `AllocateBankName "board": AllocateBankName failed (out of slots?)`. Read `game/overlord/common/sbank.cpp` directly - `InitBanks()` reserves 6 fixed bank slots at boot: `gBanks[0]`="common", `gBanks[1]`="gun", `gBanks[2]`="board", `gBanks[3..5]`=the 3 rotating level banks. But `AllocateBankName` only ever special-cased slot 0 ("common"/"commonj") with a direct return; every other name - including "board" and "gun" - fell through to a loop that starts at index 3, permanently skipping slots 1 and 2. Since a real level always keeps all 3 rotating slots full, any non-"common" named allocation was doomed to fail with "out of slots", even though 2 dedicated slots sat unused the entire time. This is a latent, pre-existing bug in the C++ port (present since this file was first written, unrelated to this mod) that nothing had ever triggered before, because nothing on Jak 2 ever requested "board" or "gun" by name through this path until the previous round's `sound-bank-load` fix started actually calling it. Fixed by adding the same direct-return special case for "gun" and "board" that "common" already had. Confirmed compiling cleanly (`cmake --build ... --target gk`). **Confirmed working in-game (8th playtest round) - board sounds now play.**
- **Charge-up sound specifically still missing (8th playtest round, after the above fix).** The launch/jump sound plays, but the looping charge-up sound (meant to build while holding L1) never does. Cause: `sound-play-by-name` (`engine/sound/gsound.gc`) doesn't generate a sound id itself - it just echoes back whatever id it's given, so a sound meant to be *updated* every frame (like this one, whose volume ramps with `charge-progress`) needs a real, unique id already sitting in `charge-sound-id` before the per-frame calls start, so the audio engine can recognize repeated calls as "update this same instance" rather than unrelated requests. Jak 3's `target-board-init` sets this up explicitly (`(set! (-> self board charge-sound-id) (new-sound-id))`, alongside 5 other sound-ids) - this line was simply never added when `charge-sound-id` was ported as a new field, unlike `charge-part` (which did get its init wired up). Fixed by adding the same `(new-sound-id)` call Jak 3 uses, in the same per-mount sound-setup block in `target-board-init`. **Not yet re-tested.**
- **Audio — root cause found and fixed (5th round, still not sufficient - see above).** Traced `og-j1-board`'s own working reference in detail (per explicit request): it doesn't just build a `.SBK`, it also patches `goal_src/jak1/engine/level/level.gc` to force-load its "board" bank exactly once, hooked into the vanilla per-tick `update-sound-banks`, guarded by a `*board-bank-loaded?*` flag — because a bank never gets loaded just by existing on disk; something has to explicitly request it. Checking Jak 2 for the equivalent request turned up nothing: `'board` doesn't appear in any of the ~40 per-continue-point `:want-sound` arrays in `engine/level/level-info.gc` (that's the actual data driving the native 3-slot bank rotation in `update-sound-banks`, `engine/level/level.gc`), and the existing `(set-setting! 'mode-sound-bank 'board 0.0 0)` call in `target-board-init` doesn't trigger a load either — its only reader in the whole codebase is a debug-HUD display line in `engine/sound/gsound.gc`. So the "board" bank — all 20 native sounds in it, not just the 4 this mod adds — was never actually being loaded during normal play; the `.SBK` structure, name-casing, and everything else checked below were never the problem. Fixed by adding an explicit, guarded `(sound-bank-load (static-sound-name "board"))` call in `target-board-init`, mirroring Jak 3's unconditional `commonj`/`common` load pattern already present natively in `gsound.gc` (a bank loaded this way sits outside the 3-slot rotation entirely, so it can't conflict with `update-sound-banks`'s own bookkeeping).
- **Audio — supporting investigation before the root cause above was found:** Exhaustively checked and *ruled out*: (a) the built `.SBK`'s name-table structure — parsed the actual output file in Python using the engine's own hash-bucket-chain format (`game/sound/989snd/loader.cpp`); all 4 new sounds are correctly present and reachable, same as the 20 native ones; (b) name-casing — `(sound-play "board-charge")` is packed byte-for-byte by `static-sound-name` (no transform), but `game/overlord/jak2/srpc.cpp:125` calls `strcpy_toupper` before the C++ lookup, which uppercases *and* converts `-` to `_` (`game/overlord/common/soundcommon.cpp:23-30`) — `"board-charge"` correctly becomes `"BOARD_CHARGE"`, matching the file; (c) bank loading — `target-board-init` already calls `(set-setting! 'mode-sound-bank 'board 0.0 0)` (native, untouched code), so the "board" bank should already load dynamically on any level. Checked directly against `og-j1-board`: their fix (standalone `build-sbk` bank + an explicit `sound-bank-load` patch) doesn't transfer here, since Jak 2 already has a native `BOARD.SBK` with a working load path — `append-sbk` remains the right choice, and its binary output is verified correct. What *was* found: the two log lines that would reveal a failed by-name lookup (`play_sound_by_name: failed to find ...`) were commented out in the engine. Reactivated and enhanced in `game/sound/989snd/player.cpp` (now also dumps every name actually registered in the bank via the pre-existing but never-called `SFXBlock::DebugPrintAllSounds()`). **This requires a full C++ engine rebuild** (`cmake --build out/build/Release --target gk`), not just `(mi)` — a verification build compiled the changed file cleanly but failed to link because `bin/common.dll` was locked (close any running REPL/`gk.exe` first). **Confirmed not a regression from that logging change:** a boot log showed many `play_sound_by_name: failed to find bank for sound ...` errors for `PORT_AMB1`, `BARGE_ENGINE`, `BOW_WASH` during the prison intro — none of these are board sounds. `git diff` on `player.cpp` shows the only change is the 2 uncommented `lg::error` lines; the bank-resolution logic itself (`GetBankByName`/`GetBankByHandle`/`GetBankWithSound`) is untouched, pre-existing code, so the board work can't be what broke this — it's a pre-existing silent failure for unrelated ambient sounds, now visible because logging is on. Still need the board-specific portion of a fresh log (after the crash fix, engine rebuild, and `(mi)`) to see whether `BOARD_CHARGE`/`BOARD_LAUNCH`/`BOARD_ZAP`/`BOARD_ZAP_HIT` fail the same way or differently.

---

## 5. Modding Changes Log

| Date | Touched/Created Files | Technical Description | Objective |
|---|---|---|---|
| 2026-08-14 | `decompiler/data/extract_sbk.{cpp,h}`<br>`goalc/build_sbk/{build_sbk,main}.{cpp,h}`<br>`goalc/make/Tools.{cpp,h}`<br>`goalc/make/MakeSystem.cpp`<br>`goalc/CMakeLists.txt`<br>`decompiler/config.{cpp,h}`<br>`decompiler/config/jak3/jak3_config.jsonc`<br>`decompiler/decompilation_process.cpp`<br>`goal_src/jak2/lib/project-lib.gp` | Backported `extract_sbk` (SPU-ADPCM extraction from `.SBK`) and `build_sbk` (audio injection into existing SBK with `SFXBlockNames` and V1/V2/V3 `SBlk` support) from `og-j1-board`, exposed via `append-sbk` macro in `project-lib.gp`. | Allow appending custom sounds to Jak 2's native `BOARD.SBK` without replacing the entire bank. |
| 2026-08-14 | `custom_assets/jak2/sounds/sfx/MODEBORD/*.wav`<br>`custom_assets/jak2/sounds/sfx/MODEBORD/metadata.txt` | Extracted `MODEBORD.SBK` from Jak 3, providing 4 new sounds for Jak 2: `BOARD_CHARGE`, `BOARD_LAUNCH` (2 stereo grains), `BOARD_ZAP`, `BOARD_ZAP_HIT`. | Provide audio source data for charged jump and zap attack. |
| 2026-08-14 | `custom_assets/jak2/models/custom_levels/jakb-jak3-board-import.glb`<br>`custom_assets/jak2/models/custom_levels/daxter-jak3-board-import.glb` | Imported 2 Jak 3 animations per character (`board-jump-high`, `board-turn-around`), remapped bone-by-bone onto native Jak 2 skeleton. Uses `jakb-lod0`/`daxter-lod0` mesh (63/48 joints, skin + 2 anims per file). | Provide the 2 missing animations for high jump and 180 turn. |
| 2026-08-14 | `goal_src/jak2/game.gp`<br>`goal_src/jak2/dgos/game.gd`<br>`goal_src/jak2/engine/data/art-elts.gc` | Declared `build-actor` targets for `jakb-jak3-board-import` and `daxter-jak3-board-import` with `:master-art-group`/`:master-ag-map` to free slots in `jakb-ag` (236/237) and `daxter-ag` (459/460); added `.go` packages to `game.gd`; declared `def-art-elt` symbols in `art-elts.gc`. | Compile custom GLBs into separate art-groups and splice into resident `jakb-ag`/`daxter-ag`. |
| 2026-08-14 | `goal_src/jak2/engine/anim/joint.gc` | Added special case in `art-group::relocate` calling `link-art!` on file load once (matching `og-j1-board` pattern for `"eichar-board+0"`). | Ensure correct dynamic link timing at boot without invoking `link-art!` during gameplay. |
| 2026-08-14 | `goal_src/jak2/engine/target/board/board-h.gc` | Extended `board-info` (`charge-sound-id`, `charge-part`, `charge-start-time`, `charge-time`, `charge-progress`, `zap-start-time`) and `target-board-bank` (`charge-jump-time`, `charge-jump-fade-time`, `charge-jump-height`, `zap-duration`, `zap-reset-time`). | Store runtime state for new mechanics in `board-info` pointer struct. |
| 2026-08-14 | `goal_src/jak2/engine/target/board/target-board.gc`<br>`goal_src/jak2/engine/target/board/board-states.gc`<br>`goal_src/jak2/engine/target/target-h.gc`<br>`goal_src/jak2/engine/target/target-handler.gc`<br>`goal_src/jak2/engine/target/target-util.gc` | Implemented gameplay logic: `L1` held charge with looping `board-charge` sound, air release impulse with `board-launch` sound and `jakb-board-jump-high-ja`; `Circle` zap attack with `board-zap`, `target-danger-set!`, i-frames, and `board-zap-hit` impact sound; `Triangle` state `target-board-turn-around` with `jakb-board-turn-around-ja` and 1s timeout guard. | Implement full 3 mechanics using native Jak 2 particle and gameplay architectures. |
| 2026-08-14 | `goalc/retarget_anim/{retarget_anim,main}.{cpp,h}`<br>`goalc/CMakeLists.txt`<br>Regenerated `{jakb,daxter}-jak3-board-import.glb` | Built standalone retargeting tool using `gltf_util` to retarget animations by bone name onto native Jak 2 skeleton (`decompiler_out/jak2/levels/common/{jakb,daxter}-lod0.glb`). Root motion on `align`/`main`, rotation only on child bones, neutral socket scale. Output GLB contains native mesh + only the 2 added animations. | Cleanly regenerate custom GLBs matching `og-j1-board` structure. |
| 2026-08-14 | `goalc/retarget_anim/retarget_anim.cpp` | Handled `JOINTS_0` conversion from `UNSIGNED_INT` to `UNSIGNED_BYTE` for `gltf_util` compatibility; clamped `daxter-lod0.glb` 62-joint skin references to 48 valid joints. | Ensure robust GLTF import without manual file hex patches. |
| 2026-08-15 | `goalc/retarget_anim/retarget_anim.cpp`<br>Regenerated both import GLBs | Fixed board shrink glitch by holding native bind-pose scale `(0.7143, 0.7143, 0.7143)` on joint 25; fixed turn-around visual glitch by removing forced 180° yaw on `align` (since rotation is already driven by `quaternion-rotate-y!` in `board-states.gc`). | Eliminate visual distortions on screen. |
| 2026-08-15 | `goal_src/jak2/engine/target/board/board-part.gc`<br>`target-board.gc` | Added `group-target-board-duck-charge` (id 1295) + `board-charge-track`, wired into `target-board-init`/`target-board-real-post` to actually spawn the charge-up glow (field existed but was never used). Added `group-board-zap-attack` (id 1296) + `board-zap-track`, replacing the wrongly-reused `group-board-spin-attack` and moving the attach point to the `hips` joint (25). Both substitute Jak 3's unavailable textures (`laser-hit2`, `laser-hit-rim`) with native Jak 2 ones (`starflash`, `water-ring`), matching `og-j1-board`'s own fix for the same gap. | Fix missing charge VFX and incorrect zap VFX reported from the first in-game test. |
| 2026-08-15 | `goal_src/jak2/engine/target/board/board-part.gc` (3 API-mismatch fixes, see below) | While re-checking after the first `(mi)` failure, found 3 Jak3-only patterns blindly carried over that would have failed compilation too: (1) `sp-group-item-flag`/`sp-group-flag` enum members are named differently in Jak2 (`bit7`/`use-local-clock`, not Jak3's `sp7`/`sp0` - same bit positions, different names, confirmed by reading both enum definitions); (2) `board-zap-track` used `(ppointer->process (-> arg1 key proc parent))`, but Jak2's `sparticle-launch-control.proc` is declared as a plain `process-drawable`, not a handle - no `.parent`/`ppointer->process` needed. Fixed and cross-checked against real enum/type declarations and existing native usage. **Did not actually fix the reported error** - see next row for the real cause. | Catch what static re-reading could catch before assuming the ordering theory below was the whole story. |
| 2026-08-15 | `goal_src/jak2/engine/data/part-groups.gc` | **Real cause of the `(mi)` failure** (a `game.gd` file-order theory was tried first and, correctly, changed nothing - reverted). `defpartgroup`'s own macro body (`sparticle-launcher-h.gc`) has `; (defconstant ,name ,id)` commented out - it only fills the `*part-group-id-table*` slot, it never binds the group's name as a usable symbol. Every native group's name (`group-target-board` included) actually resolves through a separate `(defconstant group-xxx (-> *part-group-id-table* N))` line in this file, seemingly auto-generated from retail decompilation data - which obviously doesn't have entries for brand-new groups. Added the missing declarations for `group-target-board-duck-charge` (1295) and `group-board-zap-attack` (1296), matching exactly how Jak 3 declares its own version of the first one in its own `part-groups.gc`. | Actually fix the `(mi)` failure, and record the real mechanism so it doesn't have to be re-discovered next time a new particle group is added. |
| 2026-08-15 | `game/sound/989snd/player.cpp` | Reactivated and enhanced the 2 by-name sound lookup failure logs, which were commented out in the engine — root cause of the audio bug not yet confirmed despite ruling out the `.SBK` structure, name-casing transform, and bank-loading mechanism (see §4). | Get real diagnostic signal from the next boot instead of continuing to guess. |
| 2026-08-15 | `goal_src/jak2/engine/target/board/target-board.gc` | **Real cause of the audio bug.** Traced `og-j1-board`'s working fix in detail: it force-loads its "board" bank explicitly, once, hooked into `update-sound-banks` - because nothing loads a bank just because its `.SBK` exists. Checked Jak 2's equivalent and found the same gap: `'board` is in none of `level-info.gc`'s ~40 per-continue-point `:want-sound` arrays (what actually drives the native bank rotation), and `mode-sound-bank` (set in `target-board-init`) has no reader anywhere that triggers a load - only a debug-display line in `gsound.gc`. All 20 native board sounds, not just this mod's 4, were never actually loaded. Fixed with a guarded `(sound-bank-load (static-sound-name "board"))` call in `target-board-init`, mirroring `gsound.gc`'s own unconditional `commonj`/`common` load pattern. | Make board sounds (native and new) actually audible in-game. |
| 2026-08-15 | `goalc/build_actor/common/build_actor.cpp`<br>Rebuilt `goalc`/`compiler.dll`<br>Recompiled `jakb-jak3-board-import-ag.go` and `daxter-jak3-board-import-ag.go` | **True root cause of visual glitch on jump-high and turn-around fixed:** `convert_joints` historically inserted a synthetic `"align"` joint at index 0 and shifted all GLTF joints by +1 under the assumption GLTFs lacked an align joint. Because `jakb-lod0.glb` already has `"align"`, this created 64 joints instead of 63, shifting every TransformQ joint by +1 (`main` to `waist_prog`, `waist_prog` to `upper_body`, `hips` to `Lthigh`). Fixed by detecting when `align` is already present at index 0 and using direct 0-indexed mapping, producing `num_joints = 61` matching native `jakb-ag`. | Eliminate the systemic 1-joint shift across all imported character animations in-game. |
| 2026-08-15 | `goalc/retarget_anim/retarget_anim.cpp`<br>Regenerated `{jakb,daxter}-jak3-board-import.glb` | Fixed in-game high jump mesh stretching: Jak 3's raw clip contained extreme 1.68x smear-frame scaling authored for fast 60fps bursts. When scrubbed slowly during physics ascent by `board-states.gc`, this held Jak stretched in mid-air. Fixed by holding anatomical limbs to neutral 1.0 bind scale while preserving `main` (1.4), `board`/`gun` (0.7143), and all animated rotations and dynamic translations (knees, torso, hair, board). | Eliminate character texture/mesh distortion during high jump ascent. |
| 2026-08-15 | `goal_src/jak2/engine/target/board/board-h.gc`<br>`board-part.gc`<br>`target-board.gc`<br>`game/sound/989snd/player.cpp` | Reverted the zap-part rework (see 4th round below) after it broke the zap move entirely - restored the exact crash-fix version (`part-tracker-spawn` + `sparticle-track-root`, no `zap-part` field). Scoped the C++ sound-lookup-failure log to sound names containing "BOARD" instead of logging every sound in the game, and added a one-time GOAL-side confirmation that `sound-bank-load("board")` actually executes. | Restore working zap functionality and make the sound logs usable instead of drowned in unrelated noise. |
| 2026-08-15 | `goal_src/jak2/engine/target/board/board-h.gc`<br>`board-part.gc`<br>`target-board.gc` | Fixed 2 positioning issues found after the crash fix: charge glow still landed near the head (not the board) even with the "corrected" index 24, because manually counting a GLB's skin joint list twice gave 2 different wrong answers - replaced with `(joint-node-index jakb-lod0-jg board)`, a compile-time name-based lookup against the real joint-group data. Zap effect didn't follow the board (expected, given the crash fix's static-snapshot design) - reworked to spawn a `zap-part` launch control directly on the target (new `board` struct field, created in `target-board-init` exactly like `charge-part`) instead of through `part-tracker-spawn`, so `board-zap-track`'s `key proc` resolves to a real skeleton and can keep tracking the hips joint (also now via `joint-node`) every particle-update for the whole burst. | Anchor both VFX on the correct joint and make the zap effect follow the board like Jak 3's. |
| 2026-08-15 | `goal_src/jak2/engine/target/board/board-part.gc`<br>`target-board.gc` | Fixed a crash on every zap attack: `part-tracker-spawn`'s `:target` argument produces a garbage `target-joint` in Jak 2's `part-tracker-init` (casts the macro's constant `*launch-matrix*` pointer to `int`), and the custom `board-zap-track` callback read `node-list` off the ephemeral tracker process (no skeleton) instead of Jak's — both null/out-of-bounds reads. Removed `:target` and the custom callback, replaced with `sparticle-track-root` (matches native `group-board-spin-attack`'s own call pattern). Also corrected `board-charge-track`'s joint index from 25 (`hips`) to 24 (`board`), verified against `jakb-lod0.glb`'s real skin joint order. | Fix the zap-attack crash and anchor the charge glow on the correct joint. |
| 2026-08-15 | `goal_src/jak2/engine/target/board/board-states.gc` | **Real cause of the turn-around bug, found and fixed.** The previous round's joint-index identification (assumed index 1 = `prejoint`) was wrong - re-derived from the actual GLTF parent/child hierarchy via `build_actor.cpp`'s indexing rule instead of counting the raw joint array again; index 1 is `align`, `prejoint` is index 2. Runtime logging confirmed `align delta trans` is always `(0,0,0)`, and a GLB channel dump confirmed this matches Jak 3's own source data exactly (`align` has no translation keys in either imported clip - all motion is on `main`), so the previous port was already faithful; the bug was the velocity formula treating "no signal" as "zero speed" instead of "no change". Fixed the formula to match Jak 3's exact math and added a fallback to hold entry speed when there's no root-motion signal. Also investigated the reported gun-joint drift: the joint's own data is a constant, correct bind-pose offset; the drift most likely comes from uncorrected cumulative rotation retargeting up its parent chain (`upper_body`/`waist_prog`/`main`), the probable shared cause of the jump-high stretching too - not fixed yet, flagged for a dedicated round. | Fix the turn-around's core bug (confirmed root cause) and identify the likely shared cause of 2 other visual issues. |
| 2026-08-15 | `game/overlord/jak2/srpc.cpp`<br>`game/overlord/jak2/iso_cd.cpp` | Traced the audio bug past the previous round's stopping point: `sound-bank-load` reaches `RPC_Loader`'s `load_bank` case → `LoadSoundBank` → `FS_LoadSoundBank`, which resolves "board" to `BOARD.SBK`, confirmed present in the actual build output (`out/jak2/iso/`), via the same code path proven to work for other banks in the same session. Every step of that chain was completely silent on success or failure. Added `[board-sound-debug]` logging at the 3 previously-silent points that could explain the bank still not resolving after the load call fires (`LookupBank` short-circuit, `AllocateBankName` failure, `FS_FindIN` failure). | Narrow down exactly where the load is failing, since the GOAL-side fix alone wasn't sufficient. |
| 2026-08-15 | `game/overlord/common/sbank.cpp` | **Real cause of the audio bug, found and fixed.** The new logging immediately showed `AllocateBankName "board": ... out of slots`. `InitBanks()` reserves dedicated slots for "common"/"gun"/"board" plus 3 rotating level slots (6 total), but `AllocateBankName` only ever special-cased "common" - every other name fell into a loop that only searches the 3 always-full level slots, so "board" (and "gun") could never actually be allocated, even though their dedicated slots sat unused. A latent bug nothing had triggered before, since nothing ever requested "board" by name until the previous round's fix. Added the same direct-return special case for "gun" and "board" that "common" already had. Compiles clean (`--target gk`). | Make the "board" bank actually allocatable, fixing playback for real this time. |
| 2026-08-15 | `goalc/retarget_anim/retarget_anim.cpp`<br>Regenerated both import GLBs | Implemented the 6th round's rest-pose-mismatch hypothesis for the gun-joint drift: rotation now retargeted as a delta from the source's own bind pose instead of copied absolutely, using the source skeleton's bind pose (computed the same way as the base's). Compiles clean, both GLBs regenerated. **Hypothesis then disproven**: direct comparison found Jak 2's and Jak 3's native bind-pose rotations identical `(0,0,0,1)` for the whole `gun` parent chain, making the delta computation mathematically equivalent to the old raw copy - confirmed by an FK comparison of `gun`'s position relative to `main`, old vs new, coming back byte-identical across every frame of both clips. Kept anyway (harmless, more correct in general) but doesn't fix the reported symptom; real cause still open. | Test the leading hypothesis for the gun-joint drift; ruled it out with hard data instead of leaving it unverified. |
| 2026-08-15 | `goal_src/jak2/engine/target/board/target-board.gc` | **Real cause of the missing charge-up sound, found and fixed.** `sound-play-by-name` doesn't generate a sound id, it only echoes back whatever it's given - so a per-frame-updated sound (like `board-charge`, whose volume ramps with `charge-progress`) needs a real, unique id already in place before the update loop starts, for the audio engine to recognize repeat calls as updates to the same instance. Jak 3's `target-board-init` sets `charge-sound-id` via `(new-sound-id)` alongside 5 other sound-ids; this line was simply never added when the field was ported (unlike `charge-part`, whose init was wired up correctly). Added the missing `(new-sound-id)` call. | Make the charge-up sound actually audible while holding L1. |
| 2026-08-16 | `goal_src/jak2/engine/target/board/target-board.gc`<br>`goal_src/jak2/engine/target/board/board-states.gc`<br>`goalc/retarget_anim/retarget_anim.cpp` | **True root causes of mini-jetboard, turn-around heading inversion & boost reward:**<br>1. *Mini-jetboard:* `target-board-exit` contains a hardcoded whitelist of states allowed while riding. Because `target-board-turn-around` was omitted, the engine cleared `(focus-status board)`, causing the board actor to drop to `idle` (`board-close-ja`). Added `target-board-turn-around` to the whitelist in `target-board-exit`, `target-board-pre-move`, and `target-board-real-post`.<br>2. *Autonomous heading inversion & boost:* `target-board-real-post` continuously overwritten `dir-targ` via stick steering/`turn-to-vector`. Added `turn-lockout-end-time` lockout in `:enter` to suppress stick override during the turn, guaranteed entry momentum (`fmax` with flattened `transv`), and fully synchronized `quat-for-control`, `quat`, `transv`, and `ctrl-xz-vel` to `dir-targ` on exit. Added forward acceleration boost (matching perfect spin trick boost), vibration, dust particles (`group-board-land-straight`), and `"board-boost"` audio upon completing the turn.<br>3. *Natural body motion:* Preserved authored translations on `main` in `retarget_anim.cpp` so Jak's pelvis naturally tracks leg bends during the flip without squashing or shrinking. | Restore normal board deployment, ensure autonomous 180° turnaround physics in all directions, and add perfect-trick acceleration boost feedback. |
| 2026-08-16 | `goal_src/jak2/engine/target/board/board-part.gc`<br>`goal_src/jak2/engine/target/board/target-board.gc`<br>`goal_src/jak2/engine/target/target-util.gc` | **Dynamic zap particle tracking, concentric multi-ring ripple & 1:1 Jak 3 parity:**<br>1. *Dynamic board tracking:* `board-zap-track` dynamically translates the active particle origin (`arg2`) to `*target*`'s board joint `(joint-node-index jakb-lod0-jg board)` on every tick, and `part-tracker-spawn` is supplied with `:callback part-tracker-track-target`. The expanding shockwave rings (`5489`) follow the jetboard synchronously in real time even at high velocity.<br>2. *Concentric ripples & Jak 3 parameter parity:* Configured `group-board-zap-attack` and particle `5489` with Jak 3's exact pulse structure (`:num 0.25`, `:length (seconds 0.335)`, `:scalevel-x (meters 0.16666667)`), emitting a train of 4 to 5 concentric expanding rings reaching 3.0m.<br>3. *Suppression of trick-spin FX:* Conditioned `target-board.gc` `'touched` handler to prevent spawning native `group-board-spin-attack` during `board-zap`.<br>4. *Collision radius parity:* Aligned `board-zap` attack collision sphere to exact Jak 3 parameters (`12288.0` / 3.0m attack radius, `13107.2` / 3.2m root bounding sphere). | Faithfully reproduce Jak 3's zap attack visuals and hit radius while tracking the moving jetboard dynamically. |

---

# 🇫🇷 Version Française

## 1. Description & Fonctionnalités

Ce mod porte trois mécaniques majeures du Jetboard introduites dans Jak 3 directement dans le système de jetboard natif de Jak 2 :

1. **Saut Haut Chargé (High Jump) :**
   - **Commandes :** Maintenir `L1` (accroupi sur le skate) puis relâcher `X` / Saut.
   - **Comportement :** Charge de l'énergie cinétique ascendante et propulse Jak nettement plus haut qu'un saut standard (`jakb-board-jump-high-ja`). Joue les sons de charge et de propulsion dédiés.
2. **Attaque Zap Circulaire (Dégâts de zone & I-Frames) :**
   - **Commandes :** Appuyer sur `Cercle` en ridant le skate.
   - **Comportement :** Déclenche une onde de choc électrique radiale blessant les ennemis proches avec des frames d'invulnérabilité. Joue `BOARD_ZAP` et `BOARD_ZAP_HIT`.
3. **Demi-Tour Rapide à 180° (Quick Turn-Around) :**
   - **Commandes :** Appuyer sur `Triangle` en ridant le skate.
   - **Comportement :** Exécute un pivotement instantané à 180° (`jakb-board-turn-around-ja`). À la fin de la rotation, déclenche une accélération vers l'avant avec vibration, poussière au sol et le son natif `"board-boost"` (identique au bonus d'accélération d'une figure réussie).

---

## 2. Architecture Technique & Outillage

### A. Pipeline de Reciblage d'Animation (`retarget_anim` & `CMakeLists.txt`)
Pour importer proprement les animations de Jak 3 dans Jak 2 sans recourir à des modifications manuelles fragiles dans Blender ou des éditeurs hexadécimaux, un nouvel outil C++ autonome a été intégré directement dans la suite d'outils OpenGOAL :

- **`goalc/CMakeLists.txt` :**
  - Déclare la cible exécutable `retarget_anim` (`add_executable(retarget_anim retarget_anim/main.cpp retarget_anim/retarget_anim.cpp)`).
  - Lie les bibliothèques `common`, `fmt` et `tiny_gltf` pour manipuler et réécrire directement les conteneurs binaires glTF/GLB d'OpenGOAL.
- **`goalc/retarget_anim/main.cpp` :**
  - Fournit une interface en ligne de commande (CLI via `CLI11`) permettant d'automatiser le reciblage :
    - `--base` (`-b`) : Le GLB de référence portant le maillage et le squelette natif de Jak 2 (`jakb-lod0.glb` ou `daxter-lod0.glb`).
    - `--source` (`-s`) : Le GLB source contenant les animations brutes extraites de Jak 3.
    - `--anim` (`-a`) : Les noms exacts des animations à extraire et recibler (`board-jump-high`, `board-turn-around`).
    - `--root-joints` : Les os recevant la translation et rotation réelle de root motion (`align`, `main`).
    - `--neutral-scale-joints` : Les os protégés de toute déformation d'échelle (`board`).
- **`goalc/retarget_anim/retarget_anim.cpp` :**
  - **Remappage par nom d'os :** Fait correspondre chaque piste d'animation au bon os du squelette de Jak 2, indépendamment des différences d'index numérique entre les deux jeux.
  - **Suppression du stretching :** Ne conserve la translation que sur les os racines (`align`/`main`) et force tous les os enfants en rotation pure (quaternions) pour préserver les longueurs de membres d'origine.
  - **Préservation de l'échelle d'attache :** Maintient l'échelle bind-pose native du joint `board` (`(0.7143, 0.7143, 0.7143)` pour annuler le facteur `1.4` de `main`).
  - **Compatibilité glTF :** Convertit automatiquement l'accessor `JOINTS_0` de `UNSIGNED_INT` vers `UNSIGNED_BYTE` exigé par `gltf_util`, et compacte les buffers pour ne générer que les 2 animations utiles sans polluer le package.

### B. Outil d'Injection Sonore (`SBK` Tooling)
- **`decompiler/data/extract_sbk.cpp` & `goalc/build_sbk/` :** Outils C++ permettant d'extraire les sons ADPCM depuis les banques `.SBK` et de les réinjecter via la macro `append-sbk` dans `project-lib.gp`.
- **`custom_assets/jak2/sounds/sfx/MODEBORD/` :** Fournit les 4 sons de Jak 3 (`BOARD_CHARGE`, `BOARD_LAUNCH`, `BOARD_ZAP`, `BOARD_ZAP_HIT`).

### C. Injection Dynamique & Logique GOAL
- **`goal_src/jak2/engine/anim/joint.gc` :** Hook dans `art-group::relocate` pour lier dynamiquement les animations au chargement fichier (`link-art!`).
- **`goal_src/jak2/engine/target/board/board-h.gc` :** Extension de `board-info` et `target-board-bank` pour stocker le temps de charge et les durées d'effets.
- **`goal_src/jak2/engine/target/board/board-states.gc` & `target-board.gc` :** Implémentation des états de saut chargé, demi-tour rapide et attaque zap avec gestion des dégâts radiaux et invulnérabilité.

---

## 3. Commandes & Procédure de Test

1. Définir le jeu actif sur Jak 2 :
   ```bash
   task set-game-jak2
   ```
2. Construire les assets et banques audio :
   ```bash
   task extract
   ```
3. Compiler le code dans le REPL (`task repl`) :
   ```lisp
   (mi)
   ```
4. Lancer le jeu :
   ```bash
   task boot-game
   ```
5. Équiper le Jetboard (`R2`) et tester :
   - **Saut chargé :** Maintenir `L1`, relâcher `X`.
   - **Attaque Zap :** Appuyer sur `Cercle`.
   - **Demi-tour rapide :** Appuyer sur `Triangle`.

---

## 4. Statut Actuel & Investigations

- **Statut de compilation & Boot :** Compilation et boot validés sur Jak 2 avec les 3 mécaniques entièrement fonctionnelles.
- **Corrections visuelles appliquées à la source (1er test en jeu) :**
  - *Correction d'échelle du Board :* Le générateur forçait précédemment l'échelle à `(1,1,1)`. Dans la bind-pose native de Jak 2, `board` a une échelle de `(0.7143, 0.7143, 0.7143)` car c'est un enfant direct de `main` (échelle `1.4`). Corrigé en conservant l'échelle bind-pose native.
  - *Correction visuelle du demi-tour :* Suppression du forçage de lacet à 180° sur `align`, car `target-board-turn-around` (`board-states.gc`) pilote déjà le demi-tour intégralement par le code de gameplay (`quaternion-rotate-y!` et `align!`).
- **Effets de particules corrigés (2e test en jeu) :**
  - *Effet de charge manquant :* le champ `charge-part` existait mais aucun `create-launch-control` ni appel `spawn` par frame n'avait jamais été câblé. Ajout de `group-target-board-duck-charge` (id 1295) dans `board-part.gc`, création du launch-control dans `target-board-init`, et appel `spawn` par frame dans `target-board-real-post` (gardé par `charge-progress != 0`) — reproduit exactement la logique de Jak 3 (`target-board.gc:2215-2224`).
  - *Mauvais effet de zap :* le zap réutilisait par erreur `group-board-spin-attack`, l'effet natif du trick de spin, sans rapport. Ajout d'un groupe dédié `group-board-zap-attack` (id 1296), point d'attache déplacé d'un décalage calculé à la main vers le joint `hips` (index 25 côté Jak 2, 37 côté Jak 3 — même nom d'os, index différent, vérifié par inspection directe des deux squelettes), comme le fait réellement Jak 3.
  - *Textures manquantes :* les effets d'origine de Jak 3 référencent des textures (`laser-hit2`, `laser-hit-rim`) absentes des assets de Jak 2 (confirmé via `decompiler/config/jak2/ntsc_v1/tex-info.min.json`). Substituées par `starflash` et `water-ring`, déjà natives à Jak 2 (`water-ring` est déjà utilisée par `group-board-spin-attack` natif) — c'est la même stratégie de substitution que le mod `og-j1-board` pour exactement le même problème, pas un choix improvisé.
- **Note Cosmétique :** Le log console `-debug` affiche `no eye anim data for target/sidekick` lors de la lecture des nouvelles animations car les animations custom n'ont pas d'entrées dans la table des clignements d'yeux (pose neutre par défaut sans risque de crash).
- **Crash + Corrections de Joint (3e test en jeu) :**
  - *Crash de l'attaque zap (bloquant) :* l'argument `:target` de `part-tracker-spawn` est cassé dans la macro de Jak 2 — `part-tracker-init` (`generic-obs.gc`) convertit le pointeur constant `*launch-matrix*` de la macro directement en `int` pour `target-joint` dès que `:target` est fourni, quel que soit le `:mat-joint` passé, produisant un index de joint invalide et une lecture hors limites de `node-list` à chaque zap. En plus de ça, le callback de particule `board-zap-track` lisait `node-list` sur `key proc`, qui pour un effet lancé via `part-tracker-spawn` est le processus éphémère du tracker lui-même (pas de squelette, pas de `node-list`) — un second déréférencement nul indépendant. L'appel natif de Jak 3 (`:mat-joint 37` en entier brut, avec un argument de type de tracker explicite) repose sur une signature de macro différente et incompatible, donc non transposable telle quelle. Corrigé en supprimant `:target` et le callback custom, en suivant le précédent natif de Jak 2 qui fonctionne déjà (`group-board-spin-attack`, lancé de la même façon avec un simple vecteur `:mat-joint` figé et `sparticle-track-root` au lieu d'une recherche de joint custom).
  - *Effet de charge ancré sur le mauvais joint :* `board-charge-track` utilisait l'index de joint 25 pour « board », mais l'inspection directe de l'ordre des joints du skin de `jakb-lod0.glb` montre que l'index 24 = `board`, 25 = `hips` — l'effet suivait en réalité les hanches. Corrigé à 24.
- **Corrections de positionnement (4e test en jeu) :** le correctif du crash a visiblement fonctionné (effet de charge visible, effet de zap conforme à Jak 3), mais a révélé 2 problèmes plus précis :
  - *L'effet de charge reste mal positionné même à l'index 24* (atterrit près de la tête, pas du board) — preuve que compter à la main les entrées de la liste de joints du skin d'un GLB ne correspond pas de façon fiable à l'index réel compilé de ce joint-group (faux deux fois : 25, puis 24). Remplacement de l'index en dur de `board-charge-track` et de celui du déclenchement du zap par `(joint-node-index jakb-lod0-jg board)` / `(joint-node jakb-lod0-jg hips)` — une résolution par nom à la compilation contre les vraies données du joint-group `jakb-lod0-jg`, le même mécanisme que toute autre recherche de joint native dans le jeu, au lieu d'un nombre compté à la main.
  - *L'effet de zap ne suit pas le board* — attendu vu la conception du correctif du crash (une position figée capturée une seule fois, comme le fait nativement `group-board-spin-attack` pour un effet possédé par `part-tracker-spawn`). Tentative de correctif : passage à un launch-control `zap-part` dédié créé directement sur la cible (comme `charge-part`) et lancé une seule fois par déclenchement, pour que `board-zap-track` puisse relire en toute sécurité la position réelle du joint hips à chaque mise à jour de particule. **Ceci a complètement cassé le zap (mouvement et effet ont tous deux disparu) et a été annulé** - voir le 5e test en jeu ci-dessous.
- **Régression (5e test en jeu) : la refonte zap-part ci-dessus a complètement cassé le mouvement.** `board-h.gc`/`board-part.gc`/`target-board.gc` restaurés exactement à la version du 4e test (correctif du crash) : `part-tracker-spawn` avec un simple vecteur `:mat-joint` figé, `sparticle-track-root` comme `:func` de particule, pas de champ `zap-part`. Mécanisme probable (non totalement confirmé, mais cohérent avec le symptôme) : `zap-part` était un champ de structure tout nouveau, contrairement à `charge-part` (ajouté plus tôt, déjà éprouvé sur plusieurs sessions) - la garde d'initialisation paresseuse `(if (zero? (-> self board zap-part)) (create-launch-control ...))` suppose que cette mémoire démarre à zéro la première fois, mais un champ fraîchement inséré dans une structure de processus mise en pool peut démarrer avec le contenu résiduel de ce qui occupait cette mémoire auparavant. Si le test de zéro a lu ça comme non-nul, `create-launch-control` ne se serait jamais exécuté, laissant `zap-part` pointer vers des données invalides - lancer via ce pointeur n'aurait alors rien fait silencieusement plutôt que de crasher, ce qui correspond mieux à « a disparu » qu'à un crash.
- **Nettoyage des logs audio (5e test en jeu) :** le log générique d'échec de résolution par nom ajouté il y a 2 tests se déclenchait pour chaque son du jeu, noyant les lignes réellement pertinentes sous des échecs constants, sans rapport, préexistants, pour des sons d'ambiance (`PORT_AMB1`, `BARGE_ENGINE`, `BOW_WASH`, ...) n'ayant aucun lien avec ce mod. Log de `player.cpp` restreint pour ne se déclencher que si le nom du son contient « BOARD » (une simple vérification de sous-chaîne suffit - `sound_name` est déjà mis en majuscules par `strcpy_toupper` avant cet appel RPC). Ajout également d'un log GOAL d'une seule ligne, une seule fois par session, confirmant que l'appel `sound-bank-load("board")` du correctif précédent s'exécute bien - le seul côté C++ ne peut pas distinguer « la banque board se charge bien, seuls les noms de sons individuels ne correspondent pas » de « l'appel de chargement ne s'exécute même jamais ».
- **Demi-tour : le momentum ne s'inverse pas, le personnage s'enfonce dans le sol, l'animation semble cassée** — investigation approfondie, non résolue à ce moment-là. Comparaison ligne par ligne de `target-board-turn-around` avec l'état natif de Jak 3 : les 3 différences apparentes (`compute-alignment!` vs `compute-delta-align!`, la formule de vélocité enveloppée dans `target-align-vel-z-adjust`, `dynam` vs `standard-dynamics`) se sont toutes révélées être des adaptations correctes et nécessaires à l'API de Jak 2. La formule d'inversion de vélocité lit `(-> self align delta trans z)`, que `compute-alignment!` dérive du joint d'index 1, alors cru être `prejoint`. Relancé `retarget_anim` avec `prejoint` ajouté à `--root-joints` pour tester l'hypothèse évidente - aucun changement. **Cette identification du joint s'est révélée fausse - voir le 6e cycle ci-dessous.** Ajout d'un log de diagnostic temporaire (préfixe `[turn-around-debug]`) pour obtenir de vraies valeurs d'exécution au prochain test.
- **Saut chargé : le personnage semble anormalement étiré pendant toute l'animation** — pas encore investigué jusqu'à une conclusion. Confirmé que la branche « grand saut » de `target-board-jump` n'utilise pas du tout `compute-alignment!`/`align!` - la hauteur vient de la physique (`transv`/gravité), la vitesse de lecture de l'animation elle-même étant ajustée via `root-channel 0 param 0/1` pour rester visuellement synchronisée avec l'arc physique (`time-to-apex`). C'est donc une catégorie de problème différente de celle du demi-tour ci-dessus - très probablement une fidélité de reciblage de rotation/pose sur des joints de membres individuels ne convenant pas aux proportions de Jak 2, plutôt qu'un bug de root-motion/alignement. Aucun correctif tenté pour l'instant.
- **Demi-tour — cause réelle trouvée et corrigée (6e test en jeu).** L'identification de `prejoint` pour le joint d'index 1 au 4e cycle était fausse - obtenue en comptant le tableau brut de joints du GLB, la même erreur déjà commise deux fois sur le joint `board`/`hips`. Reconstruction de la correspondance à partir de la vraie hiérarchie parent/enfant du GLTF (pas de la position dans le tableau), via la règle d'indexation propre de `build_actor.cpp` (index de joint en jeu = index de joint du skin GLTF + 1, `goalc/build_actor/common/build_actor.cpp`) : l'index 1 est en réalité **`align`** (sans parent ni enfant - vraiment isolé, comme on le pensait déjà), pas `prejoint`, qui est l'index 2 (sans parent dans le skin non plus, mais avec `main` comme unique enfant). Le log `[turn-around-debug]` a confirmé que `align delta trans` vaut exactement `(0,0,0)` à chaque frame de chaque tentative de demi-tour ; un dump direct des canaux du GLB importé a ensuite confirmé que c'est fidèle aux données sources de Jak 3 elles-mêmes — `align` ne porte aucune clé de translation dans les deux clips importés, tout le vrai mouvement racine est sur `main` — ce n'était donc jamais une lacune de reciblage, ni pour ce portage ni pour celui du 4e cycle. Le facteur d'échelle de la formule de vélocité étant toujours à 0, elle mettait à zéro toute la vitesse horizontale du joueur pour tout l'état au lieu de simplement la moduler : le personnage tombait tout droit sans aucun contrôle horizontal pendant jusqu'à une seconde entière à chaque tentative. Le freeze signalé après le test de ce cycle est le même bug qui s'accumule : le mouvement ne fait visiblement rien d'utile, donc appuyer de nouveau sur Triangle (réaction naturelle) le redéclenche - le log montre au moins 5 exécutions de demi-tour distinctes enchaînées les unes après les autres, `pos-y` diminuant globalement tout du long, atteignant environ -10244 (unités brutes) au moment de la capture du log, avec de brefs contacts au sol `parking-spot::find-ground` entre les tentatives plutôt qu'un atterrissage propre. Formule corrigée pour correspondre exactement aux mathématiques natives de Jak 3 (suppression du wrapper `target-align-vel-z-adjust`, que Jak 3 n'utilise pas ici) et ajout d'un repli : quand l'animation ne fournit aucun signal de root-motion, la vitesse d'entrée est conservée au lieu d'être mise à zéro, pour que le joueur continue d'avancer pendant le tour au lieu de rester bloqué sur place. C'est une adaptation délibérée et spécifique à Jak 2, pas un portage littéral de la formule de Jak 3, car la lacune de données sous-jacente (pas de mouvement sur `align`) semble inhérente au clip source de Jak 3 lui-même plutôt que quelque chose qu'un correctif de reciblage pourrait récupérer. Suppression du log `[turn-around-debug]`, désormais expliqué. **Pas encore retesté.**
- **Le joint gun semble s'éloigner du squelette au cours des deux animations importées (hypothèse du 6e cycle, testée et invalidée au 7e).** La théorie du 6e cycle - un décalage de bind-pose non corrigé entre Jak 2 et Jak 3 qui s'accumule le long de la chaîne de parenté de `gun` (`gun → upper_body → waist_prog → main → prejoint`) - a été implémentée (la rotation est désormais reciblée comme un delta par rapport à la bind-pose propre de la source au lieu d'une copie brute, `retarget_anim.cpp`, compile proprement) et régénérée dans les deux GLB, mais une vérification directe des rotations de bind-pose réelles pour chaque joint de cette chaîne, dans les `jakb-lod0.glb` natifs de Jak 2 et de Jak 3, les a trouvées **identiques** `(0,0,0,1)` (identité pure) des deux côtés. Avec des bind-pose identiques, le calcul du delta est mathématiquement équivalent à une copie brute, ce qui est exactement ce qui a été mesuré : une comparaison par cinématique directe de la position mondiale de `gun` par rapport à `main`, image par image, ancien reciblage contre nouveau, est revenue parfaitement identique. **L'hypothèse du décalage de bind-pose est fausse.** Le correctif est conservé malgré tout (inoffensif, et strictement plus correct pour toute future paire de joints ayant réellement des bind-pose différentes), mais il ne résout pas ce symptôme. La vérification par cinématique directe n'a rien révélé d'anormal non plus dans les données elles-mêmes : la distance de `gun` par rapport à `main` trace un arc lisse, continu et borné (environ 1,03 à 1,16 m sur le clip du grand saut, sans saut ni valeur aberrante) - ce qui ressemble à un balancement de bras ordinaire, pas à un bug. `gun` n'ayant aucun maillage attaché à observer dans Blender (confirmé - c'est uniquement un os), il est possible qu'il s'agisse d'un mouvement source authentique et intentionnel qui ressemble simplement à une dérive inexpliquée une fois isolé. **Une capture d'écran ou un court clip de ce à quoi cela ressemble réellement serait nécessaire pour avancer** - plus de piste d'analyse statique disponible sur ce point.
- **Audio — cause réelle trouvée et corrigée (7e test en jeu).** Le nouveau log `[board-sound-debug]` a permis de trouver immédiatement : `AllocateBankName "board": AllocateBankName failed (out of slots?)`. Lecture directe de `game/overlord/common/sbank.cpp` - `InitBanks()` réserve 6 emplacements de banque fixes au boot : `gBanks[0]`="common", `gBanks[1]`="gun", `gBanks[2]`="board", `gBanks[3..5]`=les 3 banques de niveau tournantes. Mais `AllocateBankName` ne traitait spécialement que l'emplacement 0 ("common"/"commonj") avec un retour direct ; tout autre nom - y compris "board" et "gun" - tombait dans une boucle commençant à l'index 3, sautant systématiquement les emplacements 1 et 2. Un niveau réel gardant toujours ses 3 emplacements tournants pleins, toute allocation par nom autre que "common" était vouée à échouer par « manque de place », alors que 2 emplacements dédiés restaient inutilisés en permanence. C'est un bug latent et préexistant du portage C++ (présent depuis l'écriture initiale de ce fichier, sans rapport avec ce mod) que rien n'avait jamais déclenché auparavant, car rien sur Jak 2 ne demandait « board » ou « gun » par nom via ce chemin avant que le correctif `sound-bank-load` du cycle précédent ne commence à l'appeler réellement. Corrigé en ajoutant le même cas spécial à retour direct pour "gun" et "board" que celui déjà existant pour "common". Compilation propre confirmée (`cmake --build ... --target gk`). **Confirmé fonctionnel en jeu (8e test en jeu) - les sons du board jouent désormais.**
- **Le son de charge manque toujours spécifiquement (8e test en jeu, après le correctif ci-dessus).** Le son de lancement/saut joue, mais le son de charge en boucle (censé monter pendant que L1 est maintenu) ne joue jamais. Cause : `sound-play-by-name` (`engine/sound/gsound.gc`) ne génère pas d'id lui-même - il renvoie simplement l'id qu'on lui donne, donc un son destiné à être *mis à jour* chaque frame (comme celui-ci, dont le volume monte avec `charge-progress`) a besoin d'un id réel et unique déjà présent dans `charge-sound-id` avant que les appels par frame ne commencent, pour que le moteur audio reconnaisse les appels répétés comme « mettre à jour cette même instance » plutôt que des requêtes sans rapport. Le `target-board-init` de Jak 3 le fait explicitement (`(set! (-> self board charge-sound-id) (new-sound-id))`, avec 5 autres sound-ids) - cette ligne n'avait tout simplement jamais été ajoutée lors du portage du nouveau champ `charge-sound-id`, contrairement à `charge-part` (dont l'initialisation avait bien été câblée). Corrigé en ajoutant le même appel `(new-sound-id)` que Jak 3 utilise, dans le même bloc d'initialisation sonore par montée dans `target-board-init`. **Pas encore retesté.**
- **Audio — cause réelle trouvée et corrigée (5e cycle, toujours insuffisant - voir ci-dessus).** Analysé en détail la référence fonctionnelle `og-j1-board` (sur demande explicite) : ce mod ne se contente pas de construire un `.SBK`, il patche aussi `goal_src/jak1/engine/level/level.gc` pour forcer le chargement de sa banque « board » une seule fois, accroché à `update-sound-banks` (appelé chaque tick), protégé par un flag `*board-bank-loaded?*` — parce qu'une banque ne se charge jamais du simple fait d'exister sur le disque ; il faut que quelque chose la demande explicitement. Vérifier l'équivalent côté Jak 2 n'a rien donné : `'board` n'apparaît dans aucun des ~40 tableaux `:want-sound` par point de continuation dans `engine/level/level-info.gc` (c'est cette donnée qui pilote la rotation native à 3 emplacements de `update-sound-banks`, `engine/level/level.gc`), et l'appel existant `(set-setting! 'mode-sound-bank 'board 0.0 0)` dans `target-board-init` ne déclenche pas non plus de chargement — son seul lecteur dans tout le code est une ligne d'affichage de debug HUD dans `engine/sound/gsound.gc`. Donc la banque « board » — les 20 sons natifs qu'elle contient, pas seulement les 4 ajoutés par ce mod — n'était en réalité jamais chargée en jeu normal ; la structure du `.SBK`, la casse des noms et tout le reste vérifié ci-dessous n'ont jamais été le problème. Corrigé en ajoutant un appel explicite et protégé `(sound-bank-load (static-sound-name "board"))` dans `target-board-init`, reproduisant le schéma de chargement inconditionnel de `commonj`/`common` déjà présent nativement dans `gsound.gc` (une banque chargée ainsi reste hors de la rotation à 3 emplacements, donc ne peut pas entrer en conflit avec la gestion interne de `update-sound-banks`).
- **Audio — investigation complémentaire avant la découverte de la cause ci-dessus :** Vérifié un par un et **écartés** : (a) la structure de la table de noms du `.SBK` généré — parsé le fichier réellement produit en Python avec le format de table de hachage propre au moteur (`game/sound/989snd/loader.cpp`) ; les 4 nouveaux sons sont bien présents et accessibles, comme les 20 natifs ; (b) la conversion de casse — `(sound-play "board-charge")` est empaqueté tel quel par `static-sound-name` (aucune transformation), mais `game/overlord/jak2/srpc.cpp:125` appelle `strcpy_toupper` avant l'appel C++, qui met en majuscule **et** remplace `-` par `_` (`game/overlord/common/soundcommon.cpp:23-30`) — `"board-charge"` devient bien `"BOARD_CHARGE"`, conforme au fichier ; (c) le chargement de la banque — `target-board-init` appelle déjà `(set-setting! 'mode-sound-bank 'board 0.0 0)` (code natif non modifié), donc la banque « board » devrait déjà se charger dynamiquement sur n'importe quel niveau. Comparaison directe avec `og-j1-board` : leur solution (banque `build-sbk` autonome + patch `sound-bank-load` explicite) ne s'applique pas ici, Jak 2 ayant déjà une `BOARD.SBK` native avec un chemin de chargement fonctionnel — `append-sbk` reste le bon choix, et sa sortie binaire est vérifiée correcte. Ce qui **a** été trouvé : les 2 lignes de log qui auraient révélé un échec de résolution par nom (`play_sound_by_name: failed to find ...`) étaient commentées dans le moteur. Réactivées et enrichies dans `game/sound/989snd/player.cpp` (dump complet des noms enregistrés via `SFXBlock::DebugPrintAllSounds()`, déjà existant mais jamais appelé). **Nécessite un rebuild complet du moteur C++** (`cmake --build out/build/Release --target gk`), pas seulement `(mi)` — une compilation de vérification a réussi sur le fichier modifié mais a échoué à l'édition des liens car `bin/common.dll` était verrouillé (fermer tout REPL/`gk.exe` en cours avant de relancer). **Confirmé : ce n'est pas une régression liée à ce changement de log :** un log de boot a montré de nombreuses erreurs `play_sound_by_name: failed to find bank for sound ...` pour `PORT_AMB1`, `BARGE_ENGINE`, `BOW_WASH` durant l'intro de la prison — aucun de ces sons n'est lié au jetboard. `git diff` sur `player.cpp` montre que le seul changement est la décommentation des 2 lignes `lg::error` ; la logique de résolution de banque elle-même (`GetBankByName`/`GetBankByHandle`/`GetBankWithSound`) est du code préexistant, non modifié — le travail sur le jetboard ne peut donc pas avoir cassé ça. C'est un échec silencieux préexistant sur des sons d'ambiance sans rapport, maintenant visible parce que le log est actif. Il reste besoin de la portion du log spécifique au jetboard (après le correctif du crash, le rebuild du moteur et `(mi)`) pour voir si `BOARD_CHARGE`/`BOARD_LAUNCH`/`BOARD_ZAP`/`BOARD_ZAP_HIT` échouent de la même façon ou différemment.

---

## 5. Journal des Modifications

| Date | Fichiers touchés/créés | Description technique | Objectif |
|---|---|---|---|
| 2026-08-14 | `decompiler/data/extract_sbk.{cpp,h}`<br>`goalc/build_sbk/{build_sbk,main}.{cpp,h}`<br>`goalc/make/Tools.{cpp,h}`<br>`goalc/make/MakeSystem.cpp`<br>`goalc/CMakeLists.txt`<br>`decompiler/config.{cpp,h}`<br>`decompiler/config/jak3/jak3_config.jsonc`<br>`decompiler/decompilation_process.cpp`<br>`goal_src/jak2/lib/project-lib.gp` | Portage de `extract_sbk` et `build_sbk` depuis `og-j1-board`, exposé via la macro `append-sbk` dans `project-lib.gp`. | Permettre l'injection de sons custom dans la banque native `BOARD.SBK` de Jak 2 sans la remplacer. |
| 2026-08-14 | `custom_assets/jak2/sounds/sfx/MODEBORD/*.wav`<br>`custom_assets/jak2/sounds/sfx/MODEBORD/metadata.txt` | Extraction de `MODEBORD.SBK` de Jak 3 fournissant les 4 sons : `BOARD_CHARGE`, `BOARD_LAUNCH`, `BOARD_ZAP`, `BOARD_ZAP_HIT`. | Fournir la source audio pour le saut chargé et le zap. |
| 2026-08-14 | `custom_assets/jak2/models/custom_levels/jakb-jak3-board-import.glb`<br>`custom_assets/jak2/models/custom_levels/daxter-jak3-board-import.glb` | Import de 2 animations Jak 3 par personnage (`board-jump-high`, `board-turn-around`), remappées sur le squelette natif de Jak 2 avec `jakb-lod0`/`daxter-lod0`. | Fournir les 2 animations manquantes pour le saut chargé et le demi-tour. |
| 2026-08-14 | `goal_src/jak2/game.gp`<br>`goal_src/jak2/dgos/game.gd`<br>`goal_src/jak2/engine/data/art-elts.gc` | Déclaration de `build-actor` avec `:master-art-group`/`:master-ag-map` sur des slots libres dans `jakb-ag` (236/237) et `daxter-ag` (459/460) ; ajout des `.go` à `game.gd` ; déclaration des symboles dans `art-elts.gc`. | Compiler les GLB custom et les intégrer aux art-groups résidents. |
| 2026-08-14 | `goal_src/jak2/engine/anim/joint.gc` | Hook dans `art-group::relocate` appelant `link-art!` au chargement du fichier (pattern `og-j1-board`). | Garantir la liaison dynamique au boot sans appel dangereux pendant le gameplay. |
| 2026-08-14 | `goal_src/jak2/engine/target/board/board-h.gc` | Extension de `board-info` et `target-board-bank` pour stocker l'état des nouvelles mécaniques. | Gérer les compteurs et paramètres temporels du Jetboard. |
| 2026-08-14 | `goal_src/jak2/engine/target/board/target-board.gc`<br>`goal_src/jak2/engine/target/board/board-states.gc`<br>`goal_src/jak2/engine/target/target-h.gc`<br>`goal_src/jak2/engine/target/target-handler.gc`<br>`goal_src/jak2/engine/target/target-util.gc` | Logique de gameplay : charge `L1` avec son en boucle, propulsion en l'air avec impulsion et anim `jump-high` ; zap `Cercle` avec dégâts radiaux et i-frames ; demi-tour `Triangle` avec état `turn-around`. | Implémenter les 3 mécaniques complètes de Jak 3 dans Jak 2. |
| 2026-08-14 | `goalc/retarget_anim/{retarget_anim,main}.{cpp,h}`<br>`goalc/CMakeLists.txt`<br>Régénération des GLB | Création de l'outil de reciblage automatique par nom d'os sur squelette natif Jak 2. | Régénérer proprement les GLB custom sans altération manuelle. |
| 2026-08-14 | `goalc/retarget_anim/retarget_anim.cpp` | Conversion automatique de `JOINTS_0` de `uint32` vers `uint8` et clamp de `daxter-lod0.glb` à 48 joints. | Garantir la robustesse de l'import glTF. |
| 2026-08-15 | `goalc/retarget_anim/retarget_anim.cpp`<br>Régénération des GLB | Maintien de l'échelle bind-pose native `(0.7143, 0.7143, 0.7143)` sur le joint 25 ; suppression du forçage de lacet 180° sur `align`. | Éliminer les glitchs visuels de scale et de rotation à l'écran. |
| 2026-08-15 | `goal_src/jak2/engine/target/board/target-board.gc` | **Cause réelle du bug audio.** Analyse détaillée du correctif fonctionnel d'`og-j1-board` : il force le chargement explicite de sa banque « board », une seule fois, accroché à `update-sound-banks` — parce que rien ne charge une banque du simple fait que son `.SBK` existe. Vérification de l'équivalent côté Jak 2 : même lacune trouvée — `'board` n'est présent dans aucun des ~40 tableaux `:want-sound` par point de continuation de `level-info.gc` (ce qui pilote réellement la rotation native des banques), et `mode-sound-bank` (fixé dans `target-board-init`) n'a aucun lecteur qui déclenche un chargement — seulement une ligne d'affichage de debug dans `gsound.gc`. Les 20 sons natifs du board, pas seulement les 4 ajoutés par ce mod, n'étaient donc en réalité jamais chargés. Corrigé avec un appel protégé `(sound-bank-load (static-sound-name "board"))` dans `target-board-init`, reproduisant le schéma de chargement inconditionnel `commonj`/`common` déjà natif dans `gsound.gc`. | Rendre les sons du board (natifs et nouveaux) réellement audibles en jeu. |
| 2026-08-15 | `goalc/build_actor/common/build_actor.cpp`<br>Rebuild de `goalc`/`compiler.dll`<br>Recompilation de `jakb-jak3-board-import-ag.go` et `daxter-jak3-board-import-ag.go` | **Cause réelle des glitchs visuels sur jump-high et turn-around corrigée :** `convert_joints` insérait historiquement un os `"align"` synthétique à l'index 0 et décalait tous les os du GLTF de +1 en supposant que les GLTF n'avaient pas d'align. Comme `jakb-lod0.glb` possède déjà `"align"`, cela créait 64 joints au lieu de 63, décalant chaque os TransformQ de +1 (`main` vers `waist_prog`, `waist_prog` vers `upper_body`, `hips` vers `Lthigh`). Corrigé en détectant la présence d'align à l'index 0 pour utiliser une indexation directe à 0, produisant `num_joints = 61` identique aux animations natives de `jakb-ag`. | Éliminer le décalage systémique d'un os sur toutes les animations custom importées en jeu. |
| 2026-08-15 | `goalc/retarget_anim/retarget_anim.cpp`<br>Régénération de `{jakb,daxter}-jak3-board-import.glb` | Correction de l'étirement du maillage lors du grand saut : le clip brut de Jak 3 contenait une échelle de smear extrême de 1.68× conçue pour une lecture rapide à 60 FPS. Ralentie lors de l'ascension par `board-states.gc`, elle maintenait Jak étiré en l'air. Corrigé en maintenant les membres anatomiques à l'échelle neutre 1.0 de bind-pose tout en préservant `main` (1.4), `board`/`gun` (0.7143), et toutes les rotations et translations dynamiques (genoux, torse, cheveux, board). | Éliminer les déformations de texture et de maillage pendant l'ascension du grand saut. |
| 2026-08-15 | `goal_src/jak2/engine/target/board/board-h.gc`<br>`board-part.gc`<br>`target-board.gc`<br>`game/sound/989snd/player.cpp` | Annulation de la refonte zap-part (voir 4e cycle ci-dessous) après qu'elle a complètement cassé le mouvement de zap - restauration exacte de la version du correctif du crash (`part-tracker-spawn` + `sparticle-track-root`, pas de champ `zap-part`). Log d'échec de résolution de son C++ restreint aux noms de sons contenant « BOARD » au lieu de logger chaque son du jeu, et ajout d'une confirmation GOAL unique que `sound-bank-load("board")` s'exécute bien. | Restaurer le fonctionnement du zap et rendre les logs audio exploitables plutôt que noyés dans du bruit sans rapport. |
| 2026-08-15 | `goal_src/jak2/engine/target/board/board-h.gc`<br>`board-part.gc`<br>`target-board.gc` | Correction de 2 problèmes de positionnement découverts après le correctif du crash : l'effet de charge restait près de la tête (pas du board) même avec l'index « corrigé » 24, car compter à la main la liste de joints d'un GLB a donné 2 réponses fausses différentes de suite — remplacé par `(joint-node-index jakb-lod0-jg board)`, une résolution par nom à la compilation contre les vraies données du joint-group. L'effet de zap ne suivait pas le board (attendu vu la conception à position figée du correctif du crash) — refonte en launch-control `zap-part` lancé directement sur la cible (nouveau champ dans la structure `board`, créé dans `target-board-init` comme `charge-part`) au lieu de passer par `part-tracker-spawn`, pour que le `key proc` de `board-zap-track` corresponde à un vrai squelette et puisse suivre le joint hips (aussi via `joint-node` désormais) à chaque mise à jour de particule pendant toute la rafale. | Ancrer les 2 effets sur le bon joint et faire suivre le board par l'effet de zap comme dans Jak 3. |
| 2026-08-15 | `goal_src/jak2/engine/target/board/board-part.gc`<br>`target-board.gc` | Correction d'un crash à chaque attaque zap : l'argument `:target` de `part-tracker-spawn` produit un `target-joint` invalide dans `part-tracker-init` de Jak 2 (conversion du pointeur constant `*launch-matrix*` de la macro en `int`), et le callback custom `board-zap-track` lisait `node-list` sur le processus éphémère du tracker (sans squelette) au lieu de celui de Jak — deux lectures nulles/hors limites. Suppression de `:target` et du callback custom, remplacés par `sparticle-track-root` (comme l'effet natif `group-board-spin-attack`). Correction également de l'index de joint de `board-charge-track`, de 25 (`hips`) à 24 (`board`), vérifié contre l'ordre réel des joints du skin de `jakb-lod0.glb`. | Corriger le crash de l'attaque zap et ancrer l'effet de charge sur le bon joint. |
| 2026-08-15 | `goal_src/jak2/engine/target/board/board-states.gc` | **Cause réelle du bug de demi-tour, trouvée et corrigée.** L'identification du joint d'index 1 du cycle précédent (supposé être `prejoint`) était fausse - reconstruite depuis la vraie hiérarchie parent/enfant du GLTF via la règle d'indexation de `build_actor.cpp` au lieu de recompter le tableau brut de joints ; l'index 1 est `align`, `prejoint` est l'index 2. Le log d'exécution a confirmé que `align delta trans` vaut toujours `(0,0,0)`, et un dump des canaux du GLB a confirmé que cela correspond exactement aux données sources de Jak 3 (`align` n'a aucune clé de translation dans les deux clips importés - tout le mouvement est sur `main`), donc le portage précédent était déjà fidèle ; le bug venait de la formule de vélocité qui traitait « aucun signal » comme « vitesse nulle » au lieu de « aucun changement ». Formule corrigée pour correspondre exactement à celle de Jak 3, avec ajout d'un repli conservant la vitesse d'entrée quand il n'y a aucun signal de root-motion. Investigation également de la dérive signalée du joint gun : les données propres du joint sont un décalage bind-pose constant et correct ; la dérive vient très probablement d'un reciblage de rotation cumulatif non corrigé le long de sa chaîne de parenté (`upper_body`/`waist_prog`/`main`), cause probable partagée avec l'étirement du grand saut - non corrigé, signalé pour un cycle dédié. | Corriger le bug central du demi-tour (cause racine confirmée) et identifier la cause probable commune à 2 autres problèmes visuels. |
| 2026-08-15 | `game/overlord/jak2/srpc.cpp`<br>`game/overlord/jak2/iso_cd.cpp` | Suivi du bug audio au-delà du point d'arrêt du cycle précédent : `sound-bank-load` atteint le cas `load_bank` de `RPC_Loader` → `LoadSoundBank` → `FS_LoadSoundBank`, qui résout « board » vers `BOARD.SBK`, confirmé présent dans la sortie de build réelle (`out/jak2/iso/`), via le même chemin de code dont le fonctionnement est prouvé pour d'autres banques dans la même session. Chaque étape de cette chaîne était totalement silencieuse en succès comme en échec. Ajout de logs `[board-sound-debug]` aux 3 points jusqu'ici silencieux pouvant expliquer que la banque reste non résolue après le déclenchement du chargement (court-circuit `LookupBank`, échec `AllocateBankName`, échec `FS_FindIN`). | Cerner précisément où le chargement échoue, le correctif côté GOAL seul n'ayant pas suffi. |
| 2026-08-15 | `game/overlord/common/sbank.cpp` | **Cause réelle du bug audio, trouvée et corrigée.** Le nouveau log a immédiatement montré `AllocateBankName "board": ... out of slots`. `InitBanks()` réserve des emplacements dédiés pour "common"/"gun"/"board" plus 3 emplacements de niveau tournants (6 au total), mais `AllocateBankName` ne traitait spécialement que "common" - tout autre nom tombait dans une boucle qui ne cherche que les 3 emplacements de niveau, toujours pleins, donc "board" (et "gun") ne pouvaient jamais être alloués, alors que leurs emplacements dédiés restaient inutilisés. Un bug latent que rien n'avait déclenché avant que le correctif du cycle précédent ne demande enfin "board" par nom. Ajout du même cas spécial à retour direct pour "gun" et "board" que celui déjà existant pour "common". Compile proprement (`--target gk`). | Rendre la banque « board » réellement allouable, corrigeant enfin la lecture audio. |
| 2026-08-15 | `goalc/retarget_anim/retarget_anim.cpp`<br>Régénération des deux GLB importés | Implémentation de l'hypothèse du 6e cycle sur le décalage de bind-pose pour la dérive du joint gun : la rotation est désormais reciblée comme un delta par rapport à la bind-pose propre de la source au lieu d'être copiée telle quelle, en utilisant la bind-pose du squelette source (calculée comme celle de la base). Compile proprement, les deux GLB régénérés. **Hypothèse ensuite invalidée** : comparaison directe montrant que les rotations de bind-pose natives de Jak 2 et de Jak 3 sont identiques `(0,0,0,1)` sur toute la chaîne de parenté de `gun`, rendant le calcul du delta mathématiquement équivalent à l'ancienne copie brute - confirmé par une comparaison en cinématique directe de la position de `gun` par rapport à `main`, ancien contre nouveau, parfaitement identique sur chaque image des deux clips. Conservé malgré tout (inoffensif, plus correct en général) mais ne corrige pas le symptôme signalé ; cause réelle toujours ouverte. | Tester l'hypothèse principale pour la dérive du joint gun ; écartée avec des données concrètes plutôt que laissée non vérifiée. |
| 2026-08-15 | `goal_src/jak2/engine/target/board/target-board.gc` | **Cause réelle du son de charge manquant, trouvée et corrigée.** `sound-play-by-name` ne génère pas d'id de son, il renvoie simplement celui qu'on lui donne - donc un son mis à jour chaque frame (comme `board-charge`, dont le volume monte avec `charge-progress`) a besoin d'un id réel et unique déjà en place avant que la boucle de mise à jour ne commence, pour que le moteur audio reconnaisse les appels répétés comme des mises à jour de la même instance. Le `target-board-init` de Jak 3 initialise `charge-sound-id` via `(new-sound-id)` avec 5 autres sound-ids ; cette ligne n'avait tout simplement jamais été ajoutée lors du portage de ce champ (contrairement à `charge-part`, dont l'initialisation avait bien été câblée). Ajout de l'appel `(new-sound-id)` manquant. | Rendre le son de charge réellement audible pendant que L1 est maintenu. |
| 2026-08-15 | `goal_src/jak2/engine/target/board/board-part.gc`<br>`target-board.gc` | Ajout de `group-target-board-duck-charge` (id 1295) + `board-charge-track`, câblés dans `target-board-init`/`target-board-real-post` pour réellement déclencher l'effet de charge (champ existant mais jamais utilisé). Ajout de `group-board-zap-attack` (id 1296) + `board-zap-track`, remplaçant `group-board-spin-attack` réutilisé par erreur, point d'attache déplacé vers le joint `hips` (25). Les deux substituent les textures Jak 3 indisponibles (`laser-hit2`, `laser-hit-rim`) par des textures natives Jak 2 (`starflash`, `water-ring`), comme le fait `og-j1-board` pour le même problème. | Corriger l'effet de charge manquant et l'effet de zap incorrect signalés après le premier test en jeu. |
| 2026-08-15 | `goal_src/jak2/engine/target/board/board-part.gc` (3 corrections d'API, voir ci-dessous) | En revérifiant après le premier échec de `(mi)`, 3 patterns copiés de Jak3 sans vérification qui auraient aussi fait échouer la compilation : (1) les enums `sp-group-item-flag`/`sp-group-flag` ont des noms différents côté Jak2 (`bit7`/`use-local-clock`, pas `sp7`/`sp0` de Jak3 — mêmes positions de bit, noms différents, confirmé en lisant les deux définitions d'enum) ; (2) `board-zap-track` utilisait `(ppointer->process (-> arg1 key proc parent))`, mais `sparticle-launch-control.proc` est déclaré comme un `process-drawable` simple côté Jak2, pas un handle — pas besoin de `.parent`/`ppointer->process`. Corrigés et revérifiés contre les vraies déclarations d'enum/type et l'usage natif existant. **N'a pas corrigé l'erreur réellement signalée** — voir la ligne suivante pour la vraie cause. | Corriger ce que la relecture statique pouvait attraper avant de supposer que la théorie d'ordre de fichier ci-dessous expliquait tout. |
| 2026-08-15 | `goal_src/jak2/engine/data/part-groups.gc` | **Vraie cause de l'échec de `(mi)`** (une théorie d'ordre de fichier dans `game.gd` a été essayée en premier et, effectivement, n'a rien changé — annulée). Le corps de la macro `defpartgroup` elle-même (`sparticle-launcher-h.gc`) a `; (defconstant ,name ,id)` commenté — elle ne fait que remplir la case `*part-group-id-table*`, elle ne lie jamais le nom du groupe à un symbole utilisable. Le nom de chaque groupe natif (`group-target-board` inclus) se résout en réalité via une ligne séparée `(defconstant group-xxx (-> *part-group-id-table* N))` dans ce fichier, apparemment auto-généré depuis les données de décompilation du jeu original — qui n'a évidemment aucune entrée pour des groupes tout nouveaux. Ajout des déclarations manquantes pour `group-target-board-duck-charge` (1295) et `group-board-zap-attack` (1296), exactement comme Jak 3 déclare sa propre version du premier dans son propre `part-groups.gc`. | Corriger réellement l'échec de `(mi)`, et noter le vrai mécanisme pour ne pas avoir à le redécouvrir la prochaine fois qu'un nouveau groupe de particules est ajouté. |
| 2026-08-15 | `game/sound/989snd/player.cpp` | Réactivation et enrichissement des 2 logs d'échec de résolution de son par nom, commentés dans le moteur — cause réelle du bug audio toujours non confirmée malgré l'élimination de la structure du `.SBK`, de la conversion de casse et du mécanisme de chargement de banque (voir §4). | Obtenir un vrai signal de diagnostic au prochain boot plutôt que continuer à deviner. |
| 2026-08-16 | `goal_src/jak2/engine/target/board/target-board.gc`<br>`goal_src/jak2/engine/target/board/board-states.gc`<br>`goalc/retarget_anim/retarget_anim.cpp` | **Cause réelle du mini-jetboard, de l'inversion de cap et boost d'accélération sur le demi-tour :**<br>1. *Mini-jetboard :* `target-board-exit` possède une liste blanche d'états autorisés sur skate. Comme `target-board-turn-around` n'y figurait pas, le moteur effaçait `(focus-status board)`, provoquant la fermeture du jetboard (`board-close-ja` en `idle`). Ajout de `target-board-turn-around` à la liste blanche dans `target-board-exit`, `target-board-pre-move` et `target-board-real-post`.<br>2. *Inversion de cap sans stick & boost :* `target-board-real-post` écrasait `dir-targ` à chaque frame via `turn-to-vector`/joystick. Ajout du verrou `turn-lockout-end-time` dans `:enter` pour désactiver l'interférence du stick, calcul de vitesse d'entrée garanti (`fmax` avec `transv` aplati), et synchronisation complète de `quat-for-control`, `quat`, `transv` et `ctrl-xz-vel` sur `dir-targ` à la sortie. Déclenchement d'un boost d'accélération vers l'avant (identique à une figure réussie), vibration de la manette, effet de particules de poussière (`group-board-land-straight`) et lecture du son `"board-boost"`.<br>3. *Posture naturelle :* Rétablissement des translations de `main` dans `retarget_anim.cpp` pour que le bassin suive la flexion des genoux sans tassement. | Rétablir la taille normale du skate, garantir le demi-tour autonome à 180° dans toutes les directions et ajouter le feedback de boost d'accélération. |
| 2026-08-16 | `goal_src/jak2/engine/target/board/board-part.gc`<br>`goal_src/jak2/engine/target/board/target-board.gc`<br>`goal_src/jak2/engine/target/target-util.gc` | **Suivi dynamique du jetboard par les particules de zap, train d'ondes multi-anneaux et parité 1:1 avec Jak 3 :**<br>1. *Suivi dynamique du skate :* `board-zap-track` translate dynamiquement l'origine des particules (`arg2`) sur le joint board de `*target*` `(joint-node-index jakb-lod0-jg board)` à chaque frame, et `part-tracker-spawn` est doté de `:callback part-tracker-track-target`. Les ondes de choc 3D (`5489`) suivent le jetboard en temps réel même à pleine vitesse.<br>2. *Ondes concentriques & fidélité Jak 3 :* Configuration de `group-board-zap-attack` et du sparticle `5489` calquée fidèlement sur Jak 3 (`:num 0.25`, `:length (seconds 0.335)`, `:scalevel-x (meters 0.16666667)`), générant un train de 4 à 5 anneaux concentriques successifs s'étendant jusqu'à 3.0m.<br>3. *Suppression de l'effet de spin natif :* Conditionnement du gestionnaire d'événement `'touched` dans `target-board.gc` pour empêcher le spawn de `group-board-spin-attack` lors du zap.<br>4. *Parité exacte de la sphère de dégâts :* Calibrage de la sphère d'attaque `board-zap` dans `target-util.gc` sur les valeurs natives exactes de Jak 3 (`12288.0` / 3.0m de rayon de frappe, `13107.2` / 3.2m de sphère racine englobante). | Reproduire fidèlement le rendu visuel et la portée d'attaque du zap de Jak 3 tout en suivant dynamiquement la planche en mouvement. |
