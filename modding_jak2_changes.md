# Modding Jak 2 - Changes Log (AI-assisted)

| Date | Files | Description | Objective |
|------|-------|-------------|-----------|
| 2025-07-19 | `goal_src/jak2/levels/city/farm/yakow.gc` | Rewrote yakow behavior with Jak 1-style states: `run-away` (flee from player), `graze`/`graze-kicked` (dedicated grazing with kick reaction), improved `kicked` (transitions to flee), new `die` state (drops 5 dark eco pills). Added fields: `grazing`, `walk-run-blend`, `walk-turn-blend`, `run-mode`, `home-base`. Changed `damage-amount-from-attack` to return 1 (was 0). Set HP to 3, increased run acceleration. | Make Jak 2 yakow behave like Jak 1 yakow (flee, graze, react to kicks) and drop dark eco on death. |
| 2026-08-13 | `goal_src/jak2/levels/city/farm/yakow.gc` | `kicked` state now picks `yakow-kicked-ja` (traveling kick) vs `yakow-kicked-in-place-ja` based on `(-> self nav state travel)`, matching Jak 1's original logic. Raised `default-hit-points` from 3 to 4 (4 hits to kill). `die` state now drops 6 dark eco pills (was 5) and replaced the unverified `(launch-particles (part-id-table 72/74) ...)` death effect with `(part-tracker-spawn :group group-land-poof-drt ...)`, the standard always-resident dirt dust-poof group used elsewhere in the game, so Yakow leaves a proper dust trail on death like other entities. | Finish the "yakow killable" feature per spec: authentic Jak 1 kicked animation, 4-hit kill, 6 dark eco drops, and a real dust-trail death effect instead of a guessed particle hack. |
