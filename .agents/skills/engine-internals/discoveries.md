# Engine Internals & REPL — Long-Term Discoveries & Fixes Log

This file records verified insights, runtime crashes, memory management resolutions, and REPL pitfalls.

> **Feeding Rule:**
> Whenever you identify an undocumented behavior, a syntax trap in GOAL, or the resolution of an engine crash during mod development, append a concise entry (under 10 lines) with a code snippet into this file before concluding the task.

| Date | Branch | Author/Agent | Discovery / Fix | Code Sample |
|---|---|---|---|---|
| 2026-09-11 | master-dev | Agent | `build-release-game` rebuilds only `gk` + `goalc`, skipping decompiler and tests for ~50% faster compile time. | `task build-release-game` |
| 2026-09-12 | jak2/features/haven-city-chaos | Agent | Haven City's whole ambient population is decided by the level borrowed into `ctywide` slot 1: `lwidea` = peace, `lwideb` = Metal Head invasion, `lwidec` = roboguards. `lwide-activate` reacts to the swap. | `(set! (-> *setting-control* user-default borrow) '((ctywide 1 lwideb special)))` |
| 2026-09-12 | jak2/features/haven-city-chaos | Agent | `lwideb` also CLEARS the `target-jak` alert flag, which drops `jak` from every guard's focus collide-spec in `crimson-guard::citizen-init!`. "Guards ignore Jak" is retail behaviour, not something a mod has to write. | `(logclear! (-> gp-0 alert-state flags) (traffic-alert-flag target-jak))` |
| 2026-09-12 | jak2/features/haven-city-chaos | Agent | For a permanent borrow override, write `user-default` instead of `set-setting!`: the macro binds the setting to the calling process (a debug-menu pick-func dies and takes it with it). `borrow-eval` of `user-current` runs last in `update-task-masks`, so a setting always beats the story nodes. | `(apply-settings *setting-control*)` after writing `user-default borrow` |
| 2026-09-12 | jak2/features/haven-city-chaos | Agent | `ctywide` borrow slot 0 is unused for the whole game except "Destroy the blast bots" (`(ctywide 0 lbombbot display)`) — free real estate for a mod, with a memory budget retail already proved. | `'((ctywide 0 lbombbot display) (ctywide 1 lwideb special))` |
| 2026-09-12 | jak2/features/haven-city-chaos | Agent | `traffic-engine`'s tracker names are SWAPPED in the decomp: `citizen-tracker-array` holds vehicles, `vehicle-tracker-array` holds pedestrians. `activate-one-vehicle` is the pedestrian picker. Confirmed by `tracker-index` assignment in `reset-and-init-from-manager`. | types 0..10 get `tracker-index` 1; 11..20 get 0 |
| 2026-09-12 | jak2/features/haven-city-chaos | Agent | `traffic-type` 21 is an "invalid/none" SENTINEL (`ctywide-obs.gc`, `get-random-parking-spot-type`), not a spawnable slot — new traffic types must start at 22. The per-type arrays are 21 entries (0..20), so 21 is out of bounds too. | `(if (!= a1-5 (traffic-type traffic-type-21)) ...)` |
| 2026-09-12 | jak2/features/haven-city-chaos | Agent | Traffic spawn ratios are governed by `want-count` (→ `target-count`, the live cap `activate-by-type` enforces), NOT by the random type draw. Weight one or the other, never both. | `(set! (-> info target-count) (max 0 (+ want -1)))` |
| 2026-09-12 | jak2/features/jetpack-crimsonguard | Agent | `hover-enemy` needs a per-level `*nav-network*`, which Haven City lacks — but every network call in `hover-nav-control` methods 11/13/24 sits behind `honflags-0` (retail's "following a scripted path" bit). Setting it permanently gives free flight with no network, losing only obstacle avoidance. | `(logior! (-> this hover flags) (hover-nav-flags honflags-0))` |
| 2026-09-12 | jak2/features/haven-city-chaos | Agent | `enemy && !guard` on `process-mask` isolates city Metal Heads exactly: `citizen-enemy::citizen-init!` sets `enemy`, `crimson-guard::citizen-init!` clears it and sets `guard`, citizens have neither. No type checks needed. | `(and (logtest? (-> p mask) (process-mask enemy)) (not (logtest? (-> p mask) (process-mask guard))))` |
| 2026-09-12 | jak2/features/haven-city-chaos | Agent | Resizing a decompiled runtime-only struct (e.g. `traffic-engine`) breaks any sibling field declared with a hard-coded `:offset`. Re-express it as `:overlay-at (-> array N)` so the compiler recomputes it. | `(vehicle-tracker-array traffic-tracker :inline :overlay-at (-> tracker-array 1))` |
| 2026-09-13 | master-dev | Agent | OpenGOAL Launcher mod packaging requires binaries at root (`extractor`, `gk`, `goalc`) and `data/game/graphics/opengl_renderer/shaders` + `data/decompiler/config/`. Without shaders, `gk` crashes immediately at launch. | `cp -r game/graphics/opengl_renderer/shaders "$PKG_DIR/data/game/graphics/opengl_renderer/"` |
| 2026-09-13 | master-dev | Agent | Jak 1 could not boot with `-debug` at all: `InitMachine` derived `debug_heap_end` from a 128 MB address mask, so once `DEBUG_HEAP_START` moved to `0x14000000` for the expanded PC layout the subtraction underflowed to a ~3.8 GB heap and segfaulted. Jak 2/3 already used an explicit size; Jak 1 now does too. | `u32 debug_heap_size = jak1::DEBUG_HEAP_SIZE;` (was `debug_heap_end - DEBUG_HEAP_START`) |
| 2026-09-13 | master-dev | Agent | Raising `EE_MAIN_MEM_SIZE` is only half the job per game: `END_OF_MEMORY` in each `kernel/gcommon.gc` gates `valid?`, so while it lags behind (Jak 1 was still `#x8000000`) every pointer allocated past the old bound is rejected as a bad address. | `(defconstant END_OF_MEMORY #x20000000)` |
| 2026-09-13 | master-dev | Agent | Appending to Jak 1's debug ROOT menu during link-and-exec segfaults the boot, reproducibly. Allocating a `debug-menu` is fine; `debug-menu-append-item` on the root is not (it walks every existing item via `debug-menu-item-get-max-width`). Jak 2/3 have identical bodies and are unaffected, so a Jak 1 mods-menu port needs a deferred install. | `(debug-menu-append-item (-> *debug-menu-context* root-menu) item) ;; unsafe at link time in jak1` |
| 2026-09-13 | master-dev | Agent | `gk.exe` exit codes when driven from a script: 124 means the harness timeout killed a still-running game (i.e. success), 0 / 'Shutdown (code 2)' and 127 mean a human closed the window, 139 is a real segfault. Only 139 is a failure. | `timeout 55 gk.exe -v --game jak3 -- -boot -fakeiso -debug` |
| 2026-09-13 | master-dev | Agent | OpenGOAL Launcher custom mod source `index.json` URLs resolve from branch roots (`raw.githubusercontent.com/<repo>/<branch>/index.json`). Setting `displayName` to the branch variable slug, stripping AI disclosures from release notes, and auto-incrementing semver tags (`v1.0.0` -> `v1.0.1`) ensures clean 1-click launcher installation. | `python scripts/modding/update_mod_catalog.py --branch <branch> --next-version` |
| 2026-09-15 | master-dev | Agent | The launcher's normal boot (`-boot`) sets `MasterDebug=0` AND `DebugSegment=0`, which does three things at once: DEBUG segments are not linked, `kdebugheap` is set to NULL, and `main.gc` gates the L3+SELECT debug-menu gesture behind `*debug-segment*`. Any player-facing mod UI must therefore avoid `(declare-file (debug))`, the `debug-menu` framework, and the debug heap entirely. | `if (!MasterDebug && !DebugSegment) { kdebugheap.offset = 0; }` (kmachine.cpp) |
| 2026-09-15 | master-dev | Agent | `kmalloc` on a NULL heap does NOT fail — it silently falls back to the global heap with a `kmalloc: alloc DEBUG` line on stdout. So a per-frame `(new 'debug ...)` in a retail boot is a permanent global-heap leak, not a crash. `popup-menu.gc` had exactly that in `draw-menu`; fixed to `'stack`. | `(let ((font-ctx (new 'stack 'font-context ...)))` |
| 2026-09-15 | master-dev | Agent | `process-spawn` does not install the process state until the kernel dispatches it on the NEXT frame. `send-event` to a just-spawned process hits a stateless process and is silently dropped — the event is lost with no error. Spawn on one frame, send on a later one. | verified: same-frame `'open-menu` left `draw?=#f`; spawning a frame earlier gave `draw?=#t` |
| 2026-09-15 | master-dev | Agent | Inside a `(new 'static ...)` form, both array fields and function fields must be written INLINE. Referencing a `define`d array or a named `defun` fails with "could not be evaluated at compile time". Wrap named helpers in a one-line `lambda`. | `:on-confirm (lambda () (my-helper) (none))` — NOT `:on-confirm my-helper` |
| 2026-09-15 | master-dev | Agent | `popup-menu` (`pc/util/`) is the retail-safe menu framework: non-debug, already in `GAME.CGO` for Jak 2/3, absent in Jak 1. Jak 2's version must be ticked externally (`update-menu!` from `main.gc`) and does not touch the master mode; Jak 3's `idle` state ticks itself (it clears `menu`/`pause` from its process mask) and its event handler DOES call `set-master-mode`. | `(process-mask-clear! (-> self mask) menu pause)` (jak3 only) |
| 2026-09-16 | jak2/features/transport-ag/alert | Agent | `reset-actors` (death / checkpoint restart / `(mi)`) calls every active level's `activate-func` in `*level*` SLOT ORDER, and slots come from `level-get-most-disposable` — nothing orders `ctywide` before `lwidea`. When `lwidea` wins the lower slot, `ctywide-activate` -> `traffic-start` -> `init-params` -> `reset-and-init` runs AFTER `lwide-activate` and wipes everything it just installed: Haven City respawns completely empty and guards stop reacting to Jak's crimes. Fix: re-run `lwide-activate` on the active lwide level at the end of `init-params`. | `(dotimes (i (-> *level* length)) (let ((lev (-> *level* level i))) (when (and (= (-> lev status) 'active) (= (-> lev name) 'lwidea)) (lwide-activate lev 'life))))` |
| 2026-09-16 | jak2/features/transport-ag/alert | Agent | `reset-and-init-from-manager` is the single wipe point for BOTH halves of the city's traffic config: `object-type-info-array[0..19].level` -> #f (slot 20 alone gets 'ctywide back, so `spawn-all` skips every type) and `(reset (-> this alert-state))` -> flags cleared, which drops `target-jak` and lwideb's forced war-zone alert. Restoring only the level bindings fixes the empty city and leaves the guards deaf. | `(set! (-> a1-3 level) #f)` … `(set! (-> this object-type-info-array 20 level) 'ctywide)` … `(reset (-> this alert-state))` |
| 2026-09-16 | jak2/features/transport-ag/alert | Agent | City traffic objects (`vehicle-spawn` / `citizen-spawn`) and a bare `(process-spawn ...)` both draw from `*default-dead-pool*` with a `#x4000` stack. A menu action that sends `'kill-all` + `'spawn-all` in one frozen ('menu master mode) frame churns ~150 of those slots and starves anything else spawning right then — `process-spawn` returns `#f` silently. Never log a spawn without testing the result. | `(get-process *default-dead-pool* arg1 #x4000)` |
| 2026-09-16 | jak2/features/transport-ag/alert | Agent | `art-group-load-check` — the fallback that loads an actor's art-group from disk when it is not registered in the process's level — has its whole body inside `(when *debug-segment* ...)`. A runtime-spawned actor whose art-group lives in a DIFFERENT level than the process works in a `-debug` boot and silently fails in a retail boot: `initialize-skeleton` does `(go process-drawable-art-error "art-group")`, and that state only draws debug text, so nothing is printed. | `(defun art-group-load-check (...) (when *debug-segment* (let ((s3-1 (loado (make-file-name (file-kind art-group) arg0 arg2 #f) arg1))) …)))` |
| 2026-09-16 | jak2/features/transport-ag/alert | Agent | A process printed as `:heap N/N` (used == total) has been through `dead-pool-heap::shrink-heap`, which `run-function-in-process` calls as soon as an init ends on a `go`. So the number IS the process's total heap use after init. ~1.3 KB for a `process-drawable` means its skeleton/draw-control were never allocated — i.e. it went to `process-drawable-art-error`. A healthy one is several KB. | `#<transport transport waiting-to-run :state #f :stack 0/256 :heap 1312/1312>` |
| 2026-09-16 | jak2/features/transport-ag/alert | Agent | `skeleton-group->draw-control` resolves the art-group in `(-> pp level)`, so a runtime-spawned actor must live in the level that ships its `-ag.go` AND its tpage. lwide{a,b,c} borrow ctywide's heap, so ctywide always outlives them: publishing a ctywide art-group into the lwide dir with `set-loaded-art` is lifetime-safe, the reverse dangles. | `(set-loaded-art (-> lwide-level art-group) (art-group from ctywide))` |
